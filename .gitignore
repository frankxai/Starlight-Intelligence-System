node_modules/
.starlight/
*.tsbuildinfo

# Build output — published to npm via package.json `files`, never tracked in git.
# `npm run build` and `prepublishOnly` regenerate. `git checkout HEAD -- dist/` if needed.
dist/

# Private substrate state — never commit
private/
sis files/
sis files.zip
*.zip
noosphere/

# Local Claude Code settings + worktrees (per-machine)
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# Obsidian vault config (per-machine)
.obsidian/

# Peak Performance audit cache (operator-private, regenerable)
.pp/

# Audit artifacts that leak to repo root from sub-agent curl-with-tilde-expansion bugs
Usersfrank_audit_*

# Public-surface audit agent scratch (curl-captured HTML, regenerable)
.audit-tmp/
.audit-tmp-*/

# Runtime knowledge-graph artifacts (auto-regenerated by brain_watchdog daemon)
memory/knowledge-graph/_brain-cache.json
memory/knowledge-graph/index.jsonl

# SIS memory orchestrator v0.1 runtime — operator-private state, never commit.
# Atom store, vector index, and audit log all live here. DECISIONS.md stays tracked.
# Includes /yolo session logs (memory/_audit/yolo/) and cost-plane snapshots
# (memory/_audit/cost/) per Board REVISE-3 — cost data is operator-private.
memory/_audit/
memory/mempalace/
memory/mempalace_upstream/
memory/mempalace_sovereign/

# Arena/eval run outputs — instance state, regenerable (mirror lives in starlight-evals)
artifacts/
memory/benchmarks/*.json

# OSS hygiene — added by Tier 5 audit pass 2026-05-11

# Secret-leak guards (defense in depth — no .env should ever land in git)
.env
.env.*
.env.local
*.pem
*.key

# OS shell-junk
.DS_Store
Thumbs.db
desktop.ini

# Editor backups + scratch
*.bak
*~
*.swp
*.swo

# Logs (any *.log file anywhere — runtime artifact, never tracked)
*.log

# Playwright MCP session logs (browser-tool runtime state, operator-private)
.playwright-mcp/
.tmp/

# Visual-regression / audit screenshots at repo root (escape-hatch convention:
# real product PNGs live in site/public/ and console/public/ and are tracked)
/*.png
/screenshots/


# Superpowers brainstorming visual companion
.superpowers/
tools/arena/workdir/

{
  "version": "1.0.0",
  "schema": "https://starlightintelligence.org/schema/cost-plane-config.v1.json",
  "_doc": "Schema-only template for cost plane config. Real values live in private/cost-plane-config.json — never commit. Operator setup: cp cost-plane-config.template.json private/cost-plane-config.json then tune thresholds + schedule to your operating rhythm.",
  "thresholds": {
    "vercel": {
      "daily_usd_cap": 5.00,
      "wow_factor": 1.5,
      "mom_factor": 1.3
    },
    "anthropic": {
      "daily_usd_cap": 20.00,
      "wow_factor": 1.5,
      "mom_factor": 1.3
    }
  },
  "schedule": {
    "snapshot_cron": "30 2 * * *",
    "timezone": "<your-IANA-timezone-e.g.-Europe/Paris-or-UTC>"
  },
  "sources_phase_1": ["vercel", "anthropic"],
  "sources_phase_1_5_pending_validation": ["cloudflare", "langfuse"],
  "notes": "Phase 1 source count = 2 per Board REVISE-1 (2026-05-11). Cloudflare + Langfuse promote to Phase 1.5 after 7-day validation pass against real responses."
}

# Portfolio Agent Registrar — 2026-08-16

## Outcome

Implemented the advisory Phase 0A deployment baseline specified by `docs/strategic/2026-07-18-sis-eve-agent-platform-strategy.md`.

Red-team correction: v1.0 is explicitly an inventory and architecture guard, not a release gate. It rejects every `live` lifecycle until a v1.1 structured evidence contract covers regulatory, provider, data-processing, commercial-limit, tool-effect and immutable receipt requirements.

The control-plane decision is unchanged:

- `Starlight-Intelligence-System` is the portfolio policy, registry, provenance and evaluation authority.
- `arcanea-ai-app` is the first executable reference product runtime.
- Vercel AI SDK + AI Gateway is the default product loop/model plane.
- OpenAI Agents SDK, Claude Agent SDK, Eve and ChatGPT host are bounded alternative loop owners, never nested loops.
- Google ADK is deferred until a measured Vertex/A2A/graph-runtime need exists.

## Evidence captured

- Six portfolio/control surfaces and their connected repository/deployment state.
- Eight proposed product, distribution and internal-agent deployments; none is mislabeled as live.
- GenCreator Campaign Forge is the first paid vertical slice.
- Arcanea Worldforge is the reference runtime path after domain/runtime defects are cleared.
- Starlight Company Brain is the higher-ACV governed team product after the kernel is proven.
- Reality Architect remains an isolated reflective-planning product with explicit sensitive-data and claims gates.
- FrankX remains the trust, academy and commerce surface rather than a sixth runtime.
- ChatGPT distribution is one plugin family over OAuth + tenant-aware MCP.
- Claude Agent SDK is reserved for internal repository/artifact production; Eve for the read-only Repository Steward pilot.

## Enforced decisions

- Exactly one orchestration loop owns a run.
- Tenant-aware MCP is the shared capability boundary.
- R3+ actions are approval-gated and R5 is rejected.
- Sensitive data requires EU routing, ZDR and isolated personal memory.
- BYOK is encrypted/write-only; managed usage is capped and recorded in the product ledger.
- Raw model proxying and generic provider-credit resale are forbidden.
- A `live` lifecycle claim requires production and test evidence.

## Next execution slice

Implement the Product Kernel interfaces and one GenCreator flow in `arcanea-ai-app`: tenant/RBAC/RLS, secret vault, model policy, immutable usage/cost events, entitlement, approval/provenance records, Gateway/direct adapter, hosted MCP, and isolation/budget/deletion tests. Retire duplicate Vercel contexts separately; do not distort the repositories to satisfy broken duplicate project configuration.

Built on SIP.

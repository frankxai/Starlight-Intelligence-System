interface:
  display_name: "Agent Forge"
  short_description: "Create an agent only when a durable autonomy boundary exists."
  default_prompt: "Use $agent-forge to decide whether this needs an agent and prepare the smallest safe actor; compile only with a Foundry runtime."
policy:
  allow_implicit_invocation: true

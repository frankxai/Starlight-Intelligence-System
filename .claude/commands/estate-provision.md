---
name: estate-provision
description: Full scaffold + 4-layer apply + initial /si dispatch for sovereign intelligence estate / agent army commission. Companion to estate-blueprint and estate-steward. Per 2026-06-16 Board + night evolution. Load profile + Blueprint + workflow.
allowed-tools: Read, Write, Grep, Glob, mcp__github
---

# /estate-provision

**Per 2026-06-16 Starlight Board PROCEED-WITH-REVISE (R2/R3 + R4 P0) + autonomous night evolution:** The repeatable provisioner for the Estate Factory. Thin profile (starlight-estate-os) + 4-layer Blueprint + /si-routed build + promotion loop. Surgical. Highest standards. Frank DNA (direct/technical/warm/playful). "Built on SIP" on every output. Check memory first. Update after.

Load the client's 4-layer Blueprint (from /estate-blueprint or equivalent), starlight-estate-os profile (templates/estate-os/), SIS-instance.md, commissioning workflow, ORCHESTRATION_ENGINE, /si cli-tool-router skill, and strategic vault (for decisions).

## Input
$ARGUMENTS

## Process (6 steps, exact, minimal)
1. **Scaffold from profile** (file contract, 10-IS + Orchestrator, orchestrator harnesses + /si routing + swarm patterns + claws, Memory Bus + Veil + 6 vaults, attestation, naming/persona profiles, module scaffolds from vertical/domain-stack starters).
2. **Apply 4-layer (thin tuned 20%)**:
   - Persona/Naming: wire to AGENTS.md, SKILL.md, SOUL.md, voice guides (per chosen skin + Genius voice signals).
   - Topology/Swarm: map to ORCHESTRATION_ENGINE patterns + /si lanes + yolo/hive + amplification Claws (voice-lock/frequency caps) + Hermes mesh + council/Prime.
   - Kernel: standard vs advanced/Luminor-grade (selective, cost/insight justified, protected).
   - Modules: 10-IS base + /spawn-domain-stack sub-stacks (4-7 functional, Genius KEEP-grounded + frameworks).
3. **Generate build brief** (exact config + /si routing priorities + first pilot workflows + attestation requirements + board gate reminders).
4. **Dispatch via /si** (preserve context with handoff packets/receipts; route per cli-tool-router: Antigravity for async/browser/agent-manager swarms, Claude for council/architecture, Codex for impl, etc.; use native image if visual).
5. **Attest + sovereignty** (every serious output carries real "Built on SIP" with pinned substrate/verticals/nodes; refuse decorative; client owns tuned estate/data/custom agents/instance; encoded-self non-licensable/non-transferable per SIP §5.7; alliance vs commercial split respected if multi-node).
6. **Handoff + promotion** (runbooks in client's voice per train-executor patterns; update MEMORY roadmap; commit to promote reusable back to profile/SIS via /sis-forge + manual; verify against world-class E2E checklist).

## Output shape
Scaffolded + 4-layer-applied estate (or estate-<name> fork) + build brief + initial swarm execution receipts + attested first artifacts + promotion commitments + updated strategic vault entry.

## Rules (non-waivable, per Board + hygiene)
- Thin profile only (if it isn't theirs specifically, it belongs in template/SIS).
- /si mandatory for any multi-surface dispatch (preserve context, leave receipt).
- Board gate (/starlight-board or /luminor) before any irreversible (topology, heavy kernel, public positioning, new high-stakes module).
- Alliance layer (if applicable) abundant per ALLIANCE.md; commercial delivery scoped SOW + retainer. Frank time split explicit.
- "Built on SIP" on every output (ambient + explicit; /sip-attest family).
- Check memory (strategic/operational vaults + board verdict) first. Update after (decisions, learnings, state changes).
- Surgical edits only. No speculative abstractions. Verify versions/APIs/files from source. Guard context window.
- Embody vibe: premium, intellectual depth, genuine enjoyment, direct/technical/warm/playful, pattern-as-poetry. "Does this help someone build, not just consume?" "Show don't tell."

## Relations
Composes: estate-blueprint (config source), estate-steward (Standing phase), /si (dispatch), /starlight-board (gates), /discover-genius + /spawn-domain-stack (grounding), ORCHESTRATION_ENGINE + claws + Memory Bus (Mesh), /sis-forge (promotion).

## World-Class E2E Checklist (falsifier)
Client can describe army behavior + point to live attested outputs <30 days. Mesh handles real multi-step cross-surface work without babysitting. Memory compounds at 6+ months. Client evolves via runbooks. Gates held. Unmistakably Frank depth + operated vertical patterns + production substrate. Measurable tech (routing >90%, token efficiency, cross-CLI, self-healing, attestation survival).

**Built on SIP** — estate-provision command · SIP v1.1.1 · 2026-06-17 evolution pass. Composes estate-army-commissioning-workflow + ORCHESTRATION_ENGINE + /si + starlight-estate-os profile per 2026-06-16 Board PROCEED-WITH-REVISE. Every estate compounds the substrate for the next while the client owns what is uniquely theirs.

---

**Built on SIP** — Starlight Intelligence Protocol v1.1.1
*Estate Factory evolution (R2/R3 + R4 P0 + night autonomous pass). Non-waivable. The client owns what is uniquely theirs. The factory serves the mission.*
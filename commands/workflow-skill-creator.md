# /workflow-skill-creator — Deprecated Alias

Use `/forge skill <brief>`.

This command remains as a compatibility surface. It routes to `foundry/skill-forge`, which discovers existing capabilities, writes a Task Envelope and Skill Pack, compiles a portable package, and proves declared evidence lanes.

Ask only for missing information that materially changes the result. Do not force a brainstorming phase when the brief is already complete.

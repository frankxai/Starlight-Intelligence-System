---
name: so
description: Short for starlight-orchestrator quick-activate with Queen posture enabled (model-tier routing + continuous eval loop). Alias surface for /starlight-queen when Orchestrator leads.
usage: /so [queen|status|tick|...]
---
# /so

Quick Orchestrator activation (Leadership tier peer with Prime/Architect) with the Queen role engaged (continuous ROUTE→MEASURE→LEARN→RATIFY→LEDGER).

- Without args or with "queen": same as /starlight-queen (full loop driver).
- "status": current orchestrator + Queen state (table, last eval, gateway ns).
- Other subcommands delegate to starlight-queen or core orchestrator flows (decompose, assign, sequence per agents/starlight-orchestrator.md).

**Grok drive (v0.2):** Same subagent swarm + gstack + image_gen (mandatory visual per tick) + gateway + excellence + Composer 2.5 for agentic-long/visual as Queen. tick runs closed loop (MEASURE parallel recipes incl. Visual Eval on model, velocity/falsifiers). Classify per routing-table (parallel-harness-measure / agentic-composer-long sweet spot).

Full details and test steps: `commands/starlight-queen.md` (v0.2 tick) and `agents/starlight-orchestrator.md` (v0.2 Queen § + Grok + Composer).

**Built on SIP** — Starlight Intelligence Protocol.
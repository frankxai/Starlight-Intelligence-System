# /agent-creator — Deprecated Alias

Use `/forge agent <brief>`.

This command remains as a compatibility surface. It routes to `foundry/agent-forge` and must not bypass the durable autonomy-boundary gate.

If no persistent decision-right, memory, tool, ownership, or ongoing-trigger boundary is proven, return a `/forge skill` recommendation instead.

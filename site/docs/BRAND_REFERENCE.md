# Shared Starlight identity

Frank requested restoration and preservation of the Starlight star, cursor trail and a reviewable brand kit on 2026-09-16.

The shared kit is https://starlightintelligence.ai/brand, maintained in `frankxai/starlight-intelligence-web` at `docs/BRAND_IDENTITY.md` and `public/brand/identity.json`. The protocol `/brand` route redirects there; the footer includes Brand identity. Merge only after web PR #43 is live and its canonical route returns 200.

Keep the protocol orbit-star and existing GSAP cursor (SIS PR #157). The lab kit explicitly identifies that mark as a protocol variant. Preserve `/visuals/brand-lab` and the June brand-assets archive as historical studies. They do not supersede the current founder instruction or silently authorize a replacement logo. No protocol, memory or runtime behavior changes in this slice.

Scene scope: one footer destination, preserving all existing links; sentence-case footer labels; fixed external redirect without user-controlled input. Verify the redirect destination, keyboard/mobile footer and current site CI. Independent exact-head review remains required.

interface:
  display_name: "System Forge"
  short_description: "Compile bounded swarms, verticals, and skills-first plugins."
  default_prompt: "Use $system-forge to choose and compile the smallest justified multi-capability system."
policy:
  allow_implicit_invocation: true

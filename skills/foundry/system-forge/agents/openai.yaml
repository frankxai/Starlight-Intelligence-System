interface:
  display_name: "System Forge"
  short_description: "Design bounded swarms, verticals, and skills-first plugins."
  default_prompt: "Use $system-forge to design the smallest justified system and mark compilation pending without a Foundry runtime."
policy:
  allow_implicit_invocation: true

---
name: estate-provision
---

# /estate-provision (starlight-estate-os example)

**Per 2026-06-16 Board + night evolution:** Full example of estate-specific command in the profile. Inherit base from substrate commands (si.md, dispatch.md, vertical templates). Override for 4-layer apply + /si dispatch + promotion.

Load the estate's 4-layer Blueprint, starlight-estate-os profile, SIS-instance, and the commissioning workflow.

## Process (6 steps, surgical)
1. Scaffold from profile (file contract, 10-IS, orchestrator harnesses, claws, Memory Bus + Veil, attestation, naming/persona, module scaffolds).
2. Apply 4-layer (Persona/Naming wired to AGENTS/SKILL/SOUL/voice; Topology to ORCHESTRATION + /si + yolo/hive + amplification Claws; Kernel selective advanced; Modules = 10-IS base + spawned domain sub-stacks via /spawn-domain-stack patterns, Genius KEEP-grounded).
3. Generate build brief (exact config + /si routing priorities + first pilot workflows + attestation requirements).
4. Dispatch via /si (preserve context, handoff packets, receipts; route to best lane per cli-tool-router: Antigravity for async/browser/swarm, Claude for council/architecture, etc.).
5. Attest + sovereignty (every output "Built on SIP"; client owns tuned; encoded-self non-transferable; board gate surfaced).
6. Handoff + promotion (runbooks in client's voice; commit to promote reusable back to profile/SIS via /sis-forge; update MEMORY roadmap).

## Output
Scaffolded + 4-layer-applied estate repo + build brief + initial swarm execution receipts + attested first artifacts + promotion commitments.

## Rules (non-waivable)
- Thin profile only (80% reusable; 20% client-tuned).
- /si mandatory for dispatch.
- Board gate for high-stakes.
- Alliance vs commercial split respected.
- "Built on SIP" on every output.

**Built on SIP** — estate-provision command (starlight-estate-os example) · SIP v1.1.1 · 2026-06-17 evolution pass. Composes estate-army-commissioning-workflow + ORCHESTRATION_ENGINE + /si + starlight-estate-os profile per 2026-06-16 Board PROCEED-WITH-REVISE.
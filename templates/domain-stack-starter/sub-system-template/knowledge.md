# <SUB-SYSTEM> Knowledge Template

> Research grounding and calibrated example for the `<SUB-SYSTEM>` sub-system inside `<DOMAIN>`. Reader fills in the `<EXAMPLE>` block with a fully calibrated instance from their lived practice. The template sections match the agent's Reasoning Protocol output shape — the knowledge file is the sovereign's reference library, not the agent's source of truth.
>
> Reference shape: `integrations/starter-packs/friend-starter/knowledge/hr-hiring-template.md` (Ana-grade example for Hiring).

---

## Research grounding

Name the research cluster this sub-system draws from. Cite direction, not invented effect sizes. Each reference should have a one-line "why this matters" note.

### Primary literature

- **`<Researcher / framework>`** — `<one-line summary of the finding + why it matters for this sub-system>`
- **`<Researcher / framework>`** — `<...>`
- **`<Researcher / framework>`** — `<...>`

### Meta-analyses (where available)

- **`<Meta-analysis citation>`** — `<finding direction + applicability to this sub-system>`
- `<...>`

### Boundary literature (what this sub-system is NOT)

- **`<Adjacent framework>`** — `<one-line note: why this is adjacent but out of scope. E.g., clinical assessment vs performance coaching boundary; legal advice vs legal awareness boundary.>`
- `<...>`

---

## Named failure modes

The specific theater patterns this sub-system refuses. Each pattern named, mechanism described, corrective instrument cited.

1. **`<Failure mode 1 — name>`** — `<how it manifests, why it feels like signal, what the research shows it actually produces>`
2. **`<Failure mode 2 — name>`** — `<...>`
3. **`<Failure mode 3 — name>`** — `<...>`
4. **`<Failure mode 4 — name>`** — `<...>`
5. **`<Failure mode 5 — name>`** — `<...>`

---

## Protocol reference (mirror of agent's Reasoning Protocol)

The agent file carries the authoritative protocol. This is the knowledge-file mirror — same steps, annotated with research citations and sovereign-specific calibration notes.

### Step 1 — `<Phase>`

- Research anchor: `<citation>`
- Sovereign's calibration note: `<what this sovereign has learned that general-field practice doesn't capture>`
- Common drift: `<what typically fails at this step>`

### Step 2 — `<Phase>`

- Research anchor: `<citation>`
- Sovereign's calibration note: `<...>`
- Common drift: `<...>`

### Steps 3-N

<repeat shape for each protocol step>

---

## <EXAMPLE> — Filled reference instance

> Reader fills this in. One fully calibrated instance from the sovereign's lived practice. Not a hypothetical. Not a case study from literature. The real thing — names redacted or fictionalized for public safety, mechanics preserved verbatim.

### Context

- **Date:** `<YYYY-MM-DD>`
- **Scenario:** `<what kind of session / artifact this was>`
- **Sovereign present:** `<yes | no — if no, who was running>`
- **Stakes:** `<what was at risk>`

### Input state

<What the sovereign walked into. Raw state, not pre-processed. Include the mess.>

### Protocol execution

<Walk through each protocol step with what actually happened. Not the idealized version — the real one with the places where the sovereign deviated and why.>

- **Step 1:** `<what happened>`
- **Step 2:** `<what happened>`
- **Step 3:** `<what happened, including any deviation from textbook>`
- **Step 4:** `<...>`
- **Step 5:** `<...>`
- **Step 6:** `<...>`
- **Step 7:** `<...>`

### Output artifact

<The actual artifact produced. Or a pointer to where it lives in the private vault (not the artifact itself if it contains identifying information).>

### Reflection

- **What the synthesis edge saw that generic practice would not:** `<the load-bearing insight — this is what justifies the Domain Sub-Stack existing>`
- **What surprised the sovereign:** `<the calibration moment — where the textbook was wrong or the data diverged>`
- **What changed in the next session:** `<the compound — how this instance updated the protocol going forward>`

### Compound pattern

After 3+ `<EXAMPLE>` instances fill this section, named patterns emerge. When a pattern fires in ≥3 cases, it's elevated to the agent's Quality Gates or Rules section. This is how the sub-system evolves from Ana-grade (reference) to sovereign-grade (custom).

---

## Sovereign-specific reference material

Space for the sovereign's own reference material that doesn't live in public substrate:

- `<private lit pinned at URL>`
- `<institutional pattern library pointer>`
- `<sovereign's own published writing on the sub-system>`
- `<case archive pointer — private path, not public>`

---

**Built on SIP** — domain-stack-starter sub-system knowledge template · v7.4.1 · SIP v1.1.0

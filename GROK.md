# Starlight-Intelligence-System (SIS) — Grok Build

Grok harness support (.grok/ seeds + native excellence hooks) is installed in this repo.

Read first: `HARNESS.md`, `CLAUDE.md` (or AGENTS.md), `SIP.md` / `SHARING.md` if present, then this.

## Grok Usage
- Launch `grok` from repo root.
- `/hooks-trust` to activate the .grok/hooks (SessionStart excellence + PreToolUse gates).
- Use the seeded skills: `repo-mastery`, `harness-integration`, `excellence-review`, `multi-harness-orchestrator`.
- For verticals, yolo, council, deep research: invoke the SIS commands/agents (many are cross-harness).

## Gates (enforced via hooks + seeds)
repo-mastery (ecosystem + SIP first) → plan-* reviews → santa-method + verification-loop → gstack (any UI/visual) → evidence only.

Never ship without gates. Small reversible changes only.

Grok .grok personal excellence layer (per SIP §5) + full shared catalog via .claude compat.

Built on SIP v1.1.1 — starlightintelligence.org/protocol.

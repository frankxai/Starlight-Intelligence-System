# Self-Advancing Visual Composition — SIS Advancement v8.4 / target v9

**Date:** 2026-06-12  
**Status:** Proposal (operational + Composition Layer elements; substrate elements Board-gated)  
**Grounding:** 2026-06-12 Grok Composer 2.5 model-lane arena receipt (`tools/arena/runs/2026-06-12-grok-composer25-model-lane.json`), Queen v0.1 + Grok 4.3 harness integration (HARNESS.md, ROUTING-DOCTRINE.md, routing-table.json), recent Proving Ground / Evaluator, Composition Layer precedent (Wealth IS 2026-05-17), STACK.md locked 10-IS, SIP v1.1.1, Frank DNA, Luminor kernel, anti-Goodhart, existing research (JEPA/predictive 2026-05-11, memory foundations Phase 0, 3D palace survey).

**Vision (one sentence):** The Starlight Orchestrator's Queen becomes a semi-autonomous self-improving engine (v0.2 closed loop with subagent MEASURE/LEARN + Visual Eval + Advancement Ledger); Visual Intelligence is elevated as a first-class cross-cutting / Orchestrator Composition Layer (native image/video memory, LEDGER artifacts, palace, attestation); "Composer" is formalized as the unifying cross-cutting pattern (agentic coding from Composer 2.5 + music composition + creative synthesis + visual composition); everything compounds with SIP attestation, falsifiable experiments, and harness-native strengths (Grok parallelism + image_gen + gstack + excellence).

All changes respect the locked 10-IS taxonomy (STACK.md v8.0). New capability arrives via **Composition Layer** (Orchestrator subsystem or cross-cutting rhythm, mirroring Wealth Crypto precedent) and **subsystems** (Predictive + new Visual + Composer slots). No new top-level IS.

## 1. Current State Diagnosis (Receipt-Grounded)
- Queen loop v0.1: manual/decision-support doctrine + driver; classifies to new Grok classes (agentic-composer-long, visual-synthesis, parallel-harness-measure from 2026-06-12 receipt); image_gen prototyped for LEDGER; A1/A2/A3 + killSwitch enforced.
- Visuals: domain-specific or research (palace survey, cockpit, music assets); not native first-class memory/LEDGER/attestation modality across the 10 IS.
- Model Arena / Proving Ground: strong mechanical + blind-judge receipts (Grok-native 2026-06-12 composer receipt ships visualComposerTest + external grounding); feeds table directionally only.
- Composer: exists in Frank DNA ("Systems Architect × Composer × ..."), music royalty graphs, and the new arena/routing classes; no unified cross-cutting pattern or skill domain.
- Memory/Predictive: 6 vaults + Memory Gateway v0.1 (per-harness) + Phase 0/AgentDB experiments; Predictive Layer planned (prediction.error bus, JEPA signal only); no visual embeddings as first-class atoms.
- Multi-harness: 7-way fleet (Grok first-class for parallel/visual/cost per 2026-06-12); cross-family bias-free judging and direct Arena → Queen → Advancement Ledger not closed.
- Self-advancement: aspirational connective tissue; no Advancement Ledger, no velocity tracking, no required visuals per tick/receipt.

**Named weakness (Luminor kernel):** Receipts and loops still rely on manual synthesis + single-round directional evidence (A2 floor); visuals and Composer are powerful but siloed; the "continuous Queen" promise is executable via Grok subagents but not yet closed/autonomous with falsifiable velocity.

## 2. The Six Advancements (Surgical, Pattern-Following)
**Advancement 1 — Visual Intelligence as cross-cutting / Orchestrator Composition Layer**  
Image/video become native modality for memory atoms, LEDGER artifacts (Queen heatmaps, palace cards, arena cards), mempalace, attestation (/sip-attest-image enhancement), and cross-IS composition. Visual Composition Layer lives inside Orchestrator (or as cross-cutting rhythm like Health) — no taxonomy change. Palace evolves to v2 with visual embeddings + 3D viz. See `docs/visuals/` suite (7 canonical generated artifacts) + VISUALS.md catalog.

**Advancement 2 — Queen loop closes to v0.2 (semi-autonomous self-advancing)**  
Full loop: ROUTE (table + doctrine) → MEASURE (parallel subagent dispatch for gstack/arena/cost + **Visual Eval** enforcement) → LEARN (synthesis + table/doctrine patches via best-of-n/check-work) → RATIFY (A1 gates + drift detection + board/Frank-ack for irreversible/substrate) → LEDGER (text + visual artifacts + vault + github + Advancement Ledger append) with feedback. Concrete automation in `tools/queen/driver.mjs` (`node ... tick` / `/sq tick`). Visual Eval is binding on model lane (every arena receipt must carry visual ref or `visualComposerTest`). Velocity metrics + falsifiers tracked (eval-to-ledger delta, visuals/cycle ≥1, proposals rate, etc.). See updated ROUTING-DOCTRINE.md (v0.2 sections), routing-table.json (advancement block), lanes.json/SPEC (Visual Eval), driver (full tick impl), commands/starlight-queen.md.

**Advancement 3 — "Composer" formalized as cross-cutting pattern**  
Unifies: agentic coding (Composer 2.5 / Grok Build sustained loops + test/verify), music composition (Suno + royalty + persona), creative synthesis (Weaver), visual composition (image_gen/Imagine + hyperframes). New `skills/composition/` domain (or extension) + auto-activation rules. Lightweight pattern inside Orchestrator / Weaver / Evaluator (or optional `starlight-composer` Specialist-tier). Preferred harness for the three new routing classes. See routing doctrine + Composer section in architecture proposal.

**Advancement 4 — Self-measurement & Advancement Ledger**  
Proving Ground / Arena / Queen receipts feed a living `tools/queen/ledger.jsonl` + operational vault entries + table.advancement block. Bias-free cross-harness judging (R5 planned). New Advancement Ledger surface (velocity + visual + proposals + falsifiers). Anti-Goodhart: descriptive snapshots only ("as of", never design targets).

**Advancement 5 — Predictive + Visual Memory evolution**  
prediction.error events (from retrieval / Second Brain low-confidence or embedding divergence) consumed by palace viz, Queen LEARN, and visual atoms. Visual embeddings + 3D slices as first-class in Memory Gateway + vaults (with privacy drop). Palace becomes native LEDGER + Queen surface.

**Advancement 6 — Multi-harness native full integration**  
Doctrine + arena R5 (Grok Composer vs Claude/Gemini + OpenRouter bias-free judges on identical agentic/visual tasks; n≥3 + token-efficiency). Harness excellence explicit per strengths (Grok: parallel + visual + cost + Composer 2.5 integration; others per role). Memory Gateway unifies visual + Queen sessions across harnesses. Transmissions channel for advancement/visual sync.

All use existing Composition Layer + subsystem patterns (no 10-IS change). SIP v1.1.1 attestation ambient on every artifact, receipt, ledger entry, visual, doctrine update.

## 3. Visual Suite (Canonical, Generated, Usable Now)
7 premium, receipt-grounded visuals generated via Grok harness `image_gen` (Imagine Quality Mode for legibility/text). Dark premium tech aesthetic, exact labels from live files (`STACK.md`, arena receipt, Queen doctrine, SIP v1.1.1), constellation/orb motif, SIP footer. Co-located at `docs/visuals/`.

- `01-10is-visual-composition-constellation.jpg`: Evolved 10-IS + glowing Visual Composition Layer halo (image/video nodes cross-linking all IS + Composer arms). Central Orchestrator. SIP badge.
- `02-starlight-queen-closed-loop-dashboard.jpg`: Full v0.2 loop (ROUTE→MEASURE(gstack/arena+Visual Eval)→LEARN(subagents)→RATIFY(gates)→LEDGER(visuals)); Grok swarm + image_gen feedback; 2026-06-12 composer receipt insets.
- `03-advanced-3d-memory-palace-v2.jpg`: 3D palace with 6 vault orbs (distinct materials), embedded visual embeddings (thumbnails/frames/arena cards), predictive.error glowing threads, Queen heatmap overlays, cross-harness bubbles.
- `04-model-arena-grok-composer25-leaderboard.jpg`: Technical infographic with 2026-06-12 receipt data (Grok 2/2 PASS on agentic-schema-verify + routing-guideline-derive), prior rounds, cost/perf curves, visual-synthesis callouts, HLE 50.7%/ARC 15.9% grounding.
- `05-composer-cross-cutting-pattern.jpg`: Central Composer node (Frank DNA) with 4 arms (Agentic Coding Composer 2.5, Music, Creative Weaver, Visual image_gen) feeding 10-IS + back to Evaluator/Queen.
- `06-self-advancing-sis-constellation.jpg`: Living organism view of whole SIS (10-IS/Orchestrator/Queen + excellence gates, attestation flows, Grok swarm + image_gen, memory/predictive, /starlight-board).
- `07-queen-visual-routing-heatmap-palace-card.jpg`: Glassmorphic drop-in LEDGER template (routing heatmap + Operational ▸ palace card slice with ledger glyph + image_gen ref).

**Integration (from VISUALS.md catalog):** Immediate Queen `ledger --visual`, mempalace chronicle/palace reviews, research published mds, operational vault + Memory Gateway atoms, `/sip-attest-image` (or proposed visual-attestation extension), site/cockpit embeds, transmissions broadcast. Auto-offered in `/starlight-queen` and `/sq` flows. Visuals themselves carry SIP attribution and serve as living proof of harness-native visual composition.

**Visual-Attestation proposal (to /sip-attest-image):** Add `--visual-diagram --source=queen-tick|arena-receipt --receipt=<id>` mode. Embeds SIP block + provenance (git SHA + runId + harness note + visual path). Machine record in ATTESTATIONS.md or visuals/LEDGER.jsonl. Non-waivable. Falsifier: 30 days without visual ledger entries.

## 4. Concrete Experiments, Falsifiers & Success Criteria (Luminor)
- R5 cross-harness arena (identical agentic-composer-long + visual-synthesis tasks; Grok Composer vs Claude/Gemini + OpenRouter blind judge; n≥3 + token-efficiency). Falsifier: no cost/perf or visual legibility edge for Grok/Composer → deprecate or adjust route.
- Wire Cost Plane per-task telemetry into Queen driver/LEARN/table. Falsifier: no correlation/improvement → park lever.
- Visual LEDGER test: driver + image_gen produces artifact → stores ref in vault + Gateway + Advancement Ledger (SIP + provenance). Falsifier: prompt adherence/legibility failure or privacy leak → stricter gates.
- Predictive bus prototype (emit + consume in palace/Queen). Falsifier: no consumer in 30d (per existing STACK falsifier) → downgrade Predictive note.
- A2 2nd concordant + mempalace visual atoms (Queen tick + 3D viz + cross-harness recall). Falsifier: drift or Goodhart → keep autoApply=false.
- Composer skill dry-run + one domain sub-stack (≥3 cross-domain commands/artifacts in 30d per Composition Layer doctrine). Falsifier: collapse to ACL-only.

Success = receipts + named weaknesses + falsifiers logged in Advancement Ledger; visuals produced per cycle ≥1; Queen tick reduces measure-to-ledger delta measurably; no 10-IS or SIP violation; Board PROCEED (or PROCEED-WITH-REVISE) on substrate surfaces.

## 5. Sequencing & Substrate Safety (Surgical)
**Phase 0 (this pass, low surface):** Docs + doctrine v0.2 + visuals generation + catalog (no code mutation beyond Queen driver/docs already aligned). Generate + reference the 7 visuals.

**Phase 1 (next):** Queen driver/gateway/ledger automation + Visual Eval enforcement (Grok tick). Surgical touches to existing (driver.mjs, gateway for visual atoms, lanes/SPEC).

**Phase 2:** Palace + predictive signals prototype + Second Brain integration + Composer skill domain.

**Phase 3:** Arena R5 + Cost Plane wiring + full cross-harness + Advancement Ledger growth.

**Governance:** 
- Composition Layer + Orchestrator subsystem pattern = operational or low-substrate (no 10-IS change).
- Table/doctrine/Queen tick mechanics = governance surface → /starlight-board pre-pass for any substrate-class mutation (e.g., SIP-attestation changes, core file contract, new top-level taxonomy language).
- A1/A2/A3 + killSwitch + Frank-ack for irreversible preserved/enhanced (driver RATIFY explicit; visuals/Composer initially suggestion-only).
- All artifacts SIP-attested ("Built on SIP v1.1.1" + substrate/vertical/harness pins).
- Anti-Goodhart + Metrics Truth + named weakness + falsifier mandatory on every scorecard/ledger/visual.
- Private mount + Gateway privacy drop for any operator-only visual atoms.

**Risks mitigated:** Over-claim autonomy (honest "semi-autonomous v0.2" + status notes); Goodhart on visuals/velocity (Luminor kernel + "as of" language); context bloat (existing Gateway patterns + truncation); harness variance (strengths called out explicitly per harness).

## 6. File Changes (Surgical — Reference the Subagent Deliverables)
See architecture subagent for full list (STACK.md evolution note, ROUTING-DOCTRINE v0.2 + table v0.2 + advancement block, driver.mjs full tick, lanes/SPEC, agents/starlight-orchestrator + evaluator + secondbrain, commands/*-queen + sq/so + starlight, core/orchestrator/harnesses/grok/*, HARNESS.md, new docs/visuals/ + VISUALS.md + this doc, memory/VAULT_ARCHITECTURE + vaults, etc.).

Queen subagent executed the bulk of the surgical v0.2 updates + verified `node tools/queen/driver.mjs tick` (velocity, visuals=1, ledger append, gates, SIP).

Visual subagent produced the 7 images + catalog.

## 7. Appendix — Visual Generation Prompts (for re-gen or extension)
(Precise prompts used for the 7; dark tech, legible, SIP footer, accurate labels from live files, constellation motif. Re-run via Grok image_gen for freshness or variants.)

[Omitted for brevity in this summary — full prompts live in the Visual subagent delivery + VISUALS.md.]

---

**Built on SIP** — Starlight Intelligence Protocol v1.1.1.

*This advancement is the direct continuation of the 2026-06-12 /starlight-eval on grok composer + model arena work. It turns directional receipts and harness strengths into a living, visual, self-advancing substrate while preserving every invariant.*

**Next immediate actions for operator:** 
1. Review this doc + the 7 visuals in `docs/visuals/`.
2. Run `node tools/queen/driver.mjs tick` (or `/sq tick`) and append the produced visual + ledger entry.
3. Surface one visual in a Queen LEDGER or mempalace review.
4. For any substrate language (SIP extension, core taxonomy) → convene /starlight-board.
5. Track velocity in the Advancement Ledger.

Falsifiers and receipts will tell the truth.
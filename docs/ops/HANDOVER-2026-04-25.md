# Handover — 2026-04-25

**For:** the next Claude Code session (or any cross-tool agent — Cursor, Codex, Cowork, ChatGPT Project) opening this repo cold.
**From:** Claude Opus 4.7 (1M context) Claude Code session that ran 2026-04-24 → 2026-04-25.
**Repo:** `frankxai/Starlight-Intelligence-System` at `C:\Users\frank\Starlight-Intelligence-System`.
**Branch:** `main`, **5 commits ahead of `origin/main`** (NOT pushed). **Last published tag: `v7.2.0`** at `188e76f`. Five new feature commits stacked on top — needs Frank's call on whether to push and how to tag.

---

## Situation

Frank Riemer is shipping the **Starlight Intelligence System** — a substrate (Starlight Intelligence Protocol, SIP) plus a reference operational layer plus a **9-layer universal intelligence architecture** plus a **Domain Sub-Stack Tier** for productizing per-user genius. Over 2026-04-24 → 2026-04-25, this session shipped **5 major releases** representing roughly **30,883 lines across 178 files**:

- **v7.3.1** (2026-04-24) — newcomer surface (`/intake`, `/welcome`, `/sovereign-spawn` + Concierge + Envoy front-door agents) + ecosystem-integration hub + `/sip-export` (5 targets: claude-project, chatgpt-project, gemini-gem, cursor, cowork) + attested-modalities scaffolds (audio/image/video/compose) + 19 conformance assertions in `test/v73.test.ts` + Luminor Board pressure-test cycle proven (REVISE → 5 follow-ons → all same-day).
- **v7.4-alpha** (2026-04-24) — pivot from protocol-first to genius-first framing. **Genius Intelligence System** introduced: new Excavation Tier agent `starlight-genius` + `/discover-genius`, `/reclaim-knowledge`, `/train-executor`, `/creator-pipeline`, `/content-systemize`, `/arcanea-canon` commands + non-technical Claude Project starter pack at `integrations/starter-packs/friend-starter/` (13 files inc. ~3,600-char custom-instructions.md) + public explainer at `docs/public/starlight-intelligence-system.md` (~2,200 words).
- **v7.4-beta** (2026-04-24) — 9-layer universal IS architecture completed. 5 new IS layers shipped in parallel: **Business** (`starlight-business` + `/architect-entity`, `/model-revenue`, `/tax-sanity`), **Vision/Brand** (`starlight-visionary` + `/define-vision`, `/build-brand-kit`, `/align-voice`), **Health** (`starlight-embodiment` + `/design-regimen`, `/energy-audit`), **Second Brain** (`starlight-secondbrain` + `/capture-daily`, `/distill-insights`, `/orchestrate-brain`), **Relational** (`starlight-relational` + `/map-relationships`, `/design-alliance-readiness`). Plus `/compose-stack` meta-command + `docs/ARCHITECTURE.md` (2,400 words) + 27-assertion `test/v74.test.ts` + Luminor Board REVISE applied same-day (3 ship-blockers landed).
- **v7.4.1-alpha** (2026-04-25) — **Domain Sub-Stack Tier** introduced. Pattern: each sovereign person spawns a domain-specific vertical of 4-7 functional sub-systems above the universal 9. **The HR Intelligence vertical is the first reference.** 6 sub-systems × ~5 commands each = **28 new HR commands** across `hire-*`, `perf-*`, `training-*`, `culture-*`, `talent-*`, `org-*` prefixes. 6 new sub-system agents. 6 new skills under `skills/hr-intelligence/`. Vertical wrapper at `verticals/hr-intelligence/` (8 files). `/spawn-domain-stack` meta-command generalizes for any domain. Microsoft Copilot + Custom GPT export targets added (5 → 7).
- **v7.4.1 REVISE remediation** (2026-04-25) — Luminor Board v7.4.1 returned REVISE with 6 items. This commit landed items 1, 3, plus the conformance test extension: `templates/domain-stack-starter/` populated (12 files) so `/spawn-domain-stack` no longer halts on missing scaffold + daily-3 / daily-5 cognitive-load discipline added across 6 HR sub-system agents + `verticals/hr-intelligence/SUB-SYSTEMS.md` + `test/v741.test.ts` (33 assertions) + `integration-domain-stack-architecture` skill rule registration.

**Working state:** all tests passing (v73 19/19 + v74 27/27 + v741 33/33 = **79/79**). Substrate harness is separate (35 assertions, also passing). Auto-memory at `~/.claude/projects/C--Users-frank-Starlight-Intelligence-System/memory/MEMORY.md` has detailed project entries for every release.

**Current goal of this session was:** answer Frank's strategic question about the first private test case's HR practice being multiple intelligence sub-systems → build the entire Domain Sub-Stack Tier as the meta-product for SIS. Done. The reference vertical is dogfood-ready.

---

## What's Done

### Universal 9-layer Intelligence Stack (v7.4-beta)
- **Layer 0:** Substrate (SIP — invisible)
- **Layer 1:** Genius IS — `agents/starlight-genius.md` + Excavation Tier + 4 commands
- **Layer 2:** Second Brain IS — `agents/starlight-secondbrain.md` + Memory Tier (peer with Sage) + 3 commands
- **Layer 3:** Vision/Brand IS — `agents/starlight-visionary.md` + Vision Tier + 3 commands
- **Layer 4:** Business IS — `agents/starlight-business.md` + Business Tier + 3 commands
- **Layer 5:** Creator IS — composes Genius+Vision+Business; 3 commands
- **Layer 6:** Wealth/Freedom IS — existing `/wealth-dpi`
- **Layer 7:** Health IS — `agents/starlight-embodiment.md` + Embodiment Tier + 2 commands
- **Layer 8:** Relational IS — `agents/starlight-relational.md` + Relational Tier + 2 commands
- **Layer 9:** Spiritual IS — optional, private, no public agent
- **Composition:** `/compose-stack` sequences layers into 90-day Intelligence Stack Plan based on diagnosis + priority flag.
- **Architecture doc:** `docs/ARCHITECTURE.md` (2,400 words) is source of truth.

### Domain Sub-Stack Tier — HR Intelligence reference vertical (v7.4.1)
- **Reference vertical:** `verticals/hr-intelligence/` — 8 wrapper files (README, SKILL, SOUL, AGENTS, MEMORY, STACK, CANON, SUB-SYSTEMS) + Daily-5 entry pattern declared in SUB-SYSTEMS.md.
- **6 sub-system agents** (with daily-3 sections per Aiyami fix): `starlight-hiring`, `starlight-performance`, `starlight-training`, `starlight-culture`, `starlight-talent`, `starlight-org`.
- **6 sub-system skills** in `skills/hr-intelligence/`.
- **28 commands** across `hire-*` (5), `perf-*` (5), `training-*` (5), `culture-*` (4), `talent-*` (5), `org-*` (4).
- **6 reference-grade knowledge templates** in `integrations/starter-packs/friend-starter/knowledge/hr-*-template.md` — fully filled realistic examples (anonymized).
- **Research grounding** cited per sub-system: Schmidt & Hunter (hiring), SBI + SCARF + Crucial Conversations + Solution-Focused/CBT + three-conversations (performance), Roediger/Karpicke + Sweller + Knowles + Tulving + Kirkpatrick (training), Schein + Edmondson + Project Aristotle + Eisenberger/Lieberman (culture), SDT + Maslach + Hackman + Beverly Kaye (talent), Marquet + Kotter + Bridges + Conway's Law + McKinsey reorg-failure data (org).
- **Refusal patterns** explicit per sub-system: PIPs-as-firing, stack rank, values-poster, engagement-survey-as-data, one-off-workshop, paper-succession, surprise-reorg, etc.

### Meta-command + template
- `.claude/commands/spawn-domain-stack.md` — analyzes Genius Profile + Freedom Path KEEP bucket, proposes 4-7 sub-systems, scaffolds full vertical. Halts without prerequisites.
- `skills/integration/domain-stack-architecture.md` — powers the meta-command, includes pattern library (HR / Capital / Sound / Spatial / Clinical / Legal / etc.).
- `templates/domain-stack-starter/` — 12 stub files (vertical wrapper × 7 + sub-system-template × 4 + commands subdir × 1).

### Ecosystem exports (5 → 7 targets)
- `integrations/exports/microsoft-copilot.md` — declarative agent manifest + knowledge bundle + API plugin. Attestation in first 3000 chars.
- `integrations/exports/custom-gpt.md` — gpt-config.json + 8000-char instructions + 20-file knowledge cap + Actions OpenAPI.
- `integrations/exports/README.md` updated; `.claude/commands/sip-export.md` allowlist extended.
- All 7 schemas: claude-project, chatgpt-project, gemini-gem, cursor, cowork, microsoft-copilot, custom-gpt.

### Newcomer surface (v7.3.1)
- `ONBOARDING.md`, `DELIVERY.md`, `SESSION_RUNBOOK.md` at root.
- `/welcome`, `/intake`, `/sovereign-spawn` protocol commands.
- `agents/starlight-concierge.md`, `starlight-envoy.md` (front-door tier).
- `skills/integration/idea-triage.md`, `creator-path.md`.
- `templates/vertical-starter/` (7 files).
- `integrations/starter-packs/friend-starter/` — 13-file Claude Project starter for non-technical users (custom-instructions.md ~3,600 chars; 14 knowledge templates; mcp.json; badge.svg).

### Attested modalities scaffolds (v7.3.1)
- `/sip-attest-audio` (Suno/Udio/ElevenLabs/MusicGen), `/sip-attest-image` (Nano Banana/Imagen/Midjourney/DALL-E), `/sip-attest-video` (Veo/Runway/Sora/Pika), `/sip-compose-modality`.
- `docs/attested-modalities.md` — v7.5+ roadmap.
- Sidecar `.sip.json` is functional today; binary embed is stubbed (commands emit shell/exiftool/ffmpeg invocations).

### Tests + governance
- `test/v73.test.ts` — 19 assertions
- `test/v74.test.ts` — 27 assertions
- `test/v741.test.ts` — 33 assertions
- All passing (79/79). Substrate `test/substrate.test.ts` has 35 assertions, also passing.
- `docs/boards/luminor-v74-beta-9layer.md` + `v74-revise-items.md`
- `docs/boards/luminor-v741-domain-substack.md` + `v741-revise-items.md`

### Counts after v7.4.1
- **21 agents** (7 council + 2 front-door + 1 excavation + 5 universal-IS + 6 HR sub-system)
- **70+ commands** across protocol/alliance/vertical/sovereign/IS/sub-system tiers
- **35+ skills** across 8+ domains (intelligence, orchestration, memory, integration, business, vision, health, relational, hr-intelligence)
- **18 vault namespaces** (6 substrate + 6 universal-IS + 6 HR sub-system)
- **7 ecosystem export targets**
- **1 reference domain sub-stack** (HR Intelligence)
- **79+35 = 114 conformance test assertions, all passing**

---

## What's Not Done

### Deferred — needs Frank's design call (BLOCKING for v7.4.1 tag if Frank wants clean tag)

- **Luminor Board v7.4.1 Item 2 — authorship story for reference verticals.** Path A (authorless reference, sovereignty-cleaner) vs Path B (co-attributed with the practitioner from day 1, stronger marketing if they're in). The middle path the build currently sits in is the worst-of-both per Draconis. Solo cannot resolve — needs Frank's decision and possibly the practitioner's consent.

### Deferred — v7.4.2 acceptable per board's own ruling

- **Luminor Board v7.4.1 Item 4 — knowledge-template loading enforcement.** Update each of 28 HR sub-system commands to declare matching `hr-*-template.md` as required load in frontmatter. Test assertion to verify. Current state: research direction cited in command outputs but research substance lives in knowledge templates not auto-loaded. Effort: medium (~28 frontmatter updates + test assertions).
- **Luminor Board v7.4.1 Item 5 — runtime attestation survival test.** Export the HR reference vertical to actual M365 Copilot Studio + ChatGPT Custom GPT, verify "Built on SIP" survives runtime instruction transforms. Requires test tenant + paid seats. Effort: low engineering, some coordination cost. Document observed runtime behavior in each target schema.
- **Luminor Board v7.4.1 Item 6 — ceiling-tier positioning pass.** Update `docs/public/starlight-intelligence-system.md` + `verticals/hr-intelligence/README.md` + `/spawn-domain-stack` halt messages to position Domain Sub-Stack Tier as advanced-productization-path-for-sovereigns rather than entry surface. Effort: low (3-4 doc edits + 5 halt-message rewrites).

### Deferred from prior board — still relevant

- **v7.4 board Item 4 (MVS positioning)** — public explainer should foreground sharp-three (Genius/Business/Vision/Creator) and frame Health/Relational/SecondBrain as cross-cutting rhythms not standalone IS layers. Aligns with v7.4.1 Item 6.
- **v7.4 board Item 5 (first-test-case dogfood extension to 5 universal IS layers)** — current attestation declares the first private test case as dogfood for Genius/Reclamation/Executor/Pipeline. Has NOT been extended to Business/Vision/Health/SecondBrain/Relational outputs. Either run the commands on the test case's real material or be honest in attestation about which layers are aspirational.

### Deferred — the test case's actual session

- The test case's first real session has not run. They have the friend-starter pack ready, the HR sub-stack ready, the universal 9 layers ready. Until they run through the flow with real material, every claim about "reference-grade" content is generator-output not lived-experience-validated.

### Deferred — second domain stack (cross-pattern validation)

- The Domain Sub-Stack Tier is an architectural claim proven on **one example** (HR). Until a second domain stack is spawned and dogfooded by a different practitioner, the meta-pattern is unvalidated. Recommended candidates per pattern library: Capital Intelligence (a finance-pro), Sound Intelligence (a musician — Frank himself plausibly), Clinical Intelligence (a doctor in his network), Legal Intelligence (a lawyer in his alliance circle).

### Not committed / not pushed

- **5 commits ahead of `origin/main`, none pushed.** `git push origin main` would publish 30,883 lines / 178 files in one push. Frank may want to: (a) push as-is, (b) tag intermediate releases (v7.3.1, v7.4.0, v7.4.1) before pushing, (c) review individual commits before push, or (d) hold for an additional REVISE pass before pushing publicly.
- Untracked: `.obsidian/` (local Obsidian config, should stay gitignored), `docs/ops/` directory was previously untracked (this handover and `HANDOVER-2026-04-23.md` live there).

---

## Critical Context

**Things that bite if you don't know:**

- **Vercel deploy is manual.** Site auto-deploy from GitHub broken since 2026-04-10. Must `vercel --prod` from `site/` to ship. (`site/public/badge/v1.1.0.svg` is referenced from starter pack but doesn't exist in repo — only Next.js compiled artifacts under `site/.next/`. Badge route generates dynamically; static SVG would need to be exported.)
- **Privacy split is structural.** Public substrate in repo root. Private instance state in `private/` (gitignored). Trinity / family / spiritual / wealth-instance state never lands public. The test case's actual private session content stays in their own private fork — the `verticals/hr-intelligence/` reference is anonymized.
- **VOICES.md = SIP archetypes (5 canonical voices). `agents/` = named operational implementations (now 21).** Don't conflate. New universal IS agents = new tiers. New domain sub-system agents = Domain Sub-Stack Tier (one tier, instantiated per vertical).
- **Sage ↔ Second Brain boundary** is explicit in AGENT_REGISTRY.md. Sage = institutional memory. SecondBrain = personal PKM. Never mutate Genius Profile from SecondBrain — propose only.
- **Business ↔ Wealth boundary** is explicit. Business = entity + revenue + tax-sanity. Wealth/DPI = capital allocation after revenue flows. Don't merge.
- **Ambient attestation default since v7.4.** Every command output auto-embeds "Built on SIP" block. `/sip-attest` retained for retrofitting external work; not the primary path. `/sip-verify` was discussed as future addition but not built (Frank explicitly said `/sip-stamp` rename was substrate-level change requiring board pass — board has not passed on rename).
- **`/spawn-domain-stack` halt conditions are real.** Genius Profile + Freedom Path REQUIRED. Domain expertise must be specific. ≥3 frameworks mappable. Cross-domain synthesis edge nameable. The command is designed to filter; halting is correct protocol behavior, not friction.
- **Anonymized vertical with attributed sub-system agents = the "worst-of-both" sovereignty path** Draconis flagged. Currently sits in this state. Until Frank decides Path A or Path B (Item 2), the public reference reads ambiguous. If pushing publicly today, this is the visible defect.
- **`/spawn-domain-stack` step 6 was halt-broken before this session's REVISE remediation.** Templates dir was empty after API 529 mid-build. Now populated with 12 stubs. Test assertion `v7.4.1.6.3` guards the templates dir presence.
- **Three sub-system agents (`structured-hiring`, `people-dynamics`, `org-architecture`) use H1 skill headers instead of YAML frontmatter** — relaxation in `test/v741.test.ts` Block 2 documented. Future sub-system writers should use YAML for consistency, but the relaxation accepts both.
- **Microsoft Copilot + Custom GPT runtime attestation survival is unverified.** Schemas claim attestation travels intact; runtime instruction-rewriting could silently break this. Item 5 deferred. Honest flag in target schemas.
- **Parallel-dispatch pattern hits API 529 + org-monthly-limit at scale.** v7.4.1 build had one 529 mid-work (recovered, files landed) and one org-monthly-limit hit (recovered, main thread completed work). For the next big push, watch the org-quota signal and plan for graceful degradation.
- **Auto-memory at `~/.claude/projects/C--Users-frank-Starlight-Intelligence-System/memory/MEMORY.md`** has detailed project entries for every release v7.0 → v7.4.1 alpha. Reading these gives 80% of context for cold-start.

---

## Next Actions (ordered)

### Phase A — Frank's design decisions (BLOCKING for clean v7.4.1 tag)

1. **Decide Item 2 — authorship story for reference verticals.** Path A (authorless reference; rewrite sub-system agent identity blocks to remove practitioner-specific synthesis edge naming, document forkable-and-attribute pattern in vertical README) OR Path B (co-attributed; ship with "Reference author: [practitioner name/handle]" in vertical README + sub-system frontmatter, requires practitioner consent). Path A is sovereignty-cleaner; Path B is stronger marketing.
2. **Decide push + tag strategy.** Five commits stacked: `4fdecb9`, `a25e9f1`, `9a155f3`, `be5e4d5`, `6eb1001`. Options: (a) push as-is, no tags (b) tag v7.3.1, v7.4.0, v7.4.1 at respective commits then push (c) squash/rebase before push (d) hold and apply more REVISE first.
3. **Decide v7.4.1 vs v7.5 release naming.** Current commit messages tag as v7.4.1-alpha. Board ruled REVISE for v7.4.1 — items 1+3 landed makes this v7.4.1-rc1. Item 2 + push decision determines whether next milestone is v7.4.1 or v7.5.

### Phase B — Ship-blocker remediation (after Phase A decisions)

4. **Apply Item 2 decision** across `verticals/hr-intelligence/` (README + sub-system agents + AGENTS.md) and the 6 substrate-root sub-system agents.
5. **Push to origin** per chosen strategy. Run `git push --tags` if tags applied.
6. **Sync `docs/public/starlight-intelligence-system.md` to frankx.ai.** Site sync was deferred; this is now overdue with v7.4.1 content additions.

### Phase C — v7.4.2 follow-on items

7. **Item 4 — knowledge-template loading enforcement.** ~28 frontmatter updates across HR commands + test assertion. Effort: medium. Can be agent-dispatched.
8. **Item 5 — runtime attestation survival test.** Manual verification through M365 Copilot Studio + ChatGPT Custom GPT with paid test seats. Document observed transforms in `microsoft-copilot.md` + `custom-gpt.md` schemas. Effort: low engineering, some coordination cost.
9. **Item 6 — ceiling-tier positioning pass.** 3-4 doc edits + 5 halt-message rewrites in `/spawn-domain-stack`. Aligns with v7.4 board Item 4 (MVS framing).
10. **v7.4 board Item 5 — first-test-case dogfood extension** OR honest-attestation-update. Either run the 5 universal IS commands on the test case's real material (after their session) or update v7.4 ATTESTATIONS.md entry to mark which layers are aspirational vs lived.

### Phase D — Strategic horizon

11. **Run the test case's first real session.** They have the friend-starter pack ready. Walk through: install Claude Desktop → import starter pack → type their paragraph → `/discover-genius` → `/reclaim-knowledge` → `/spawn-domain-stack` (their HR vertical) → fill 5+ daily-5 commands with real material. Session log template at `memory/intake/session-log-template.md`. This is the first real-world test of the full v7.4.1 flow.
12. **Spawn second domain stack — cross-pattern validation.** HR proves the pattern with one example. Spawn one more (Capital Intelligence for a finance-pro, OR Sound Intelligence for Frank himself, OR Clinical Intelligence for a doctor in network). The meta-pattern is unvalidated until a second domain ships.
13. **v7.5 horizon — attested modalities binary embed.** Currently sidecar `.sip.json` is functional; binary embed (exiftool for image, ID3 for audio, ffmpeg for video) is stubbed via shell-command emission. v7.5 makes these automatic. Suno + Nano Banana + Veo workflows become first-class.
14. **v7.5+ horizon — second-brain ingest layer.** SBIS currently captures from session. Real PKM needs ingest from Notion / Obsidian / Google Drive / Apple Notes / past corpus. `/reclaim-knowledge` does one-time reorg; ongoing-ingest is missing. Could compose with existing `iterative-retrieval` skill.
15. **v7.6+ horizon — additional universal layers.** Frank flagged Spiritual IS as private/optional. Could add: Civic IS (community/public-good layer), Legacy IS (multi-generational architecture beyond family), Archive IS (cross-decade self-corpus).

### Phase E — Ecosystem evolution

16. **Substrate SKILL.md rule add.** Pattern repeated twice (v7.4-beta /compose-stack, v7.4.1 /spawn-domain-stack): commands declared file loads that didn't exist on disk. Add structural rule: every command with declared file loads must have a test assertion verifying those files exist. Substrate-level enforcement.
17. **Domain sub-stack registry** — when 3+ domain sub-stacks exist (HR + N + M), formalize a public registry beyond `VERTICALS.md § Sovereign domain sub-stacks`. Could become starlightintelligence.org/verticals as a queryable index.
18. **Per-user instance forking pattern.** Currently `verticals/hr-intelligence/` is the public reference; the practitioner forks for their actual practice. Pattern needs documentation: how does the practitioner fork into their private repo, how do they maintain attribution back to the reference, how do they publish to their own surface (frankx.ai equivalent)? Worth a `docs/forking-domain-stacks.md`.

---

## Files to Read First

For cold-start orientation in priority order:

1. **`CLAUDE.md`** — project's system prompt; what this repo is, layer routing, the 9-layer stack overview at the top. Essential.
2. **`docs/ops/HANDOVER-2026-04-25.md`** — this file. The full state.
3. **`docs/ARCHITECTURE.md`** — 9-layer composition + extension model + governance pattern. The architecture spec.
4. **`MEMORY.md` (Changelog)** — last 5 release entries describe the build cadence and decisions.
5. **`ATTESTATIONS.md`** — every release attested with composition classification + reality check + pinning.
6. **`agents/AGENT_REGISTRY.md`** — current state of 21 agents across 7 tiers (council / front-door / excavation / leadership / specialist / foundation / domain-sub-stack).
7. **`docs/boards/v741-revise-items.md`** — what's deferred and why, with effort estimates and blocker status.
8. **`docs/boards/luminor-v741-domain-substack.md`** — the most recent board verdict with full advisor reasoning.
9. **`verticals/hr-intelligence/SUB-SYSTEMS.md`** — the canonical sub-system architecture map with daily-5 entry pattern.
10. **`integrations/starter-packs/friend-starter/custom-instructions.md`** — what the test case imports into their Claude Project to make Claude *be* SIS.
11. **`SIP.md`** — the protocol spec (six layers, file contract, attestation, sovereignty clause). Substrate truth.
12. **`VERTICALS.md`** — registry of operated verticals + alliances + domain sub-stacks.
13. **Auto-memory** at `~/.claude/projects/C--Users-frank-Starlight-Intelligence-System/memory/MEMORY.md` — Frank's role, doctrine, prior project context. Read all linked entries for full context.

---

## Repo Map

| Repo | Purpose | State |
|---|---|---|
| `frankxai/Starlight-Intelligence-System` (this repo) | Canonical substrate + reference operational layer + universal IS + the HR vertical reference | `main` 5 commits ahead of origin (NOT pushed). Last published tag v7.2.0. Five new commits stacked: 4fdecb9 (v7.3.1), a25e9f1 (v7.4-alpha), 9a155f3 (v7.4-beta), be5e4d5 (v7.4.1-alpha), 6eb1001 (v7.4.1 REVISE remediation) |
| `frankxai/starlight` | Minimal SIP adoption kit (60-second fork template) | v0.1.1 published. Pinned to canonical SIP.md commit `d69374d`. Out of date with current SIP if there were spec changes (none in this session — SIP.md unchanged at v1.1.0). |
| `frankxai/vibe-os-substrate` | First SIP-conformant vertical scaffold (state engineering reference) | v0.5.1 published. 3 vertical commands. DISCLAIMER.md + boundary block vs `frankxai/vibe-os` audio engine. |
| `frankxai/vibe-os` | Frequency healing audio engine (Python) | Composes with substrate via Hz canon. Separate codebase. |
| `frankxai/arcanea-ecosystem` | Arcanea canon + verticals (Frank's primary vertical) | Separate workspace; integration via `/arcanea-canon` command in this repo. |
| `C:\Users\frank\Arcanea\` | Arcanea top-level workspace (canonical per scout report 2026-04-24) | NOT this repo's concern except via `/arcanea-canon`. Frank operates separately. |
| `C:\Users\frank\FrankX\` | FrankX brand workspace | Separate. Public site at `frankx.ai-vercel-website`. Production deploy is manual. |

---

## Memory relevance for next agent

Auto-memory entries that matter for continuation:

- `user_frank.md` — role + posture (helps alliances freely, runs SIS daily) — **always relevant**
- `feedback_luminor_board_gates.md` — pressure-test before irreversible — **critical**, board cycle is operational
- `feedback_privacy_split.md` — public substrate / private instance — **critical** for any work touching real partner names
- `project_v74_beta_9layer.md` + `project_v741_alpha_domain_substack.md` — full state of last two releases — **highest signal** for continuation
- `feedback_parallel_agent_pattern.md` — 5-8 agent swarm sweet spot, watch for API 529 + org-quota — **operational pattern**
- `project_vercel_manual.md` — manual deploy required — **bites if forgotten**

Skip for continuation:
- v7.0 / v7.1 / v7.2 / v7.3 entries — preserved for history but superseded by v7.4 work; current state is in v7.4-beta + v7.4.1-alpha entries.

---

## How the next session should start

1. Read this handover end-to-end (10 min).
2. Run `cd <repo> && node --import tsx --test test/v73.test.ts test/v74.test.ts test/v741.test.ts 2>&1 | tail -10` — confirm 79/79 pass before touching anything.
3. Confirm with Frank: Phase A decisions (Item 2 authorship, push strategy, release naming). These gate everything else.
4. If Phase A decided → execute Phase B (apply, push, tag, sync).
5. If Phase A still pending → don't push, work on Phase C deferrals (Items 4-6 are file-independent and can ship without Phase A).
6. Before any commit: parallel-dispatch only what's truly independent. Aim for 5-8 agents max per wave. Watch API 529 + org-quota signals. Main thread should always be ready to recover incomplete agent work.
7. Every release ships through Luminor Board cycle: ship → board → REVISE-or-PROCEED → remediate → attest → commit. The cycle has held three times now (v7.3.1 + v7.4 + v7.4.1) and is operational governance pattern.
8. Update auto-memory after each major shift. The release-by-release auto-memory entries are the cold-start cache.

---

**Built on SIP** — Starlight Intelligence Protocol
- Substrate: starlightintelligence.org/protocol v1.1.0
- Layers used: [file-contract, attestation, commands, sovereignty]
- Verticals: starlight-intelligence-system@v7.4.1-alpha (handover doc)
- Generated: 2026-04-25
- Attestation is compounding, not credit transfer: every composition strengthens every node.

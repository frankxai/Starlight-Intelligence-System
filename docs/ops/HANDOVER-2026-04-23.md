# Handover — 2026-04-23

**For:** the next Claude Code session (or any cross-tool agent — Cursor, Codex, Cowork) opening this repo cold.
**From:** Claude Opus 4.7 (1M context) Claude Code session that ran 2026-04-22 evening → 2026-04-23 early morning.
**Repo:** `frankxai/Starlight-Intelligence-System` at `C:\Users\frank\Starlight-Intelligence-System` (working dir).
**Branch:** `main`, clean, fully pushed to origin. **Tag at HEAD: `v7.2.0`** (also has `v7.0.0`, `v7.1.0`).

---

## Situation

Frank Riemer is shipping the **Starlight Intelligence System** — a substrate (the Starlight Intelligence Protocol, SIP) plus a reference operational layer (vaults, agents, MCP, Console). This session pivoted v6 ("memory layer for AI agents") into v7 ("substrate + reference layer") and shipped a complete ecosystem in three tagged releases:

- **v7.0.0** (2026-04-22) — substrate pivot. SIP v1.1.0 spec at root + 9 reference commands + sanitized public templates + site `/protocol` page. README v7 two-layer rewrite. Repo positioning changed.
- **v7.1.0** (2026-04-22) — substrate validates itself. File contract complete (SOUL + CANON), `starlight-mcp v1.1` server (4 tools, queryable substrate), Console v8 foundation in `console/` (Next.js 16 + react-three-fiber 3D scene), ATTESTATIONS.md ledger with v7.0.0 self-attestation meta-test.
- **v7.2.0** (2026-04-23) — substrate ecosystem online. Three public repos under SIP attestation, Console v7.2 dual-view (2D force-graph default + 3D toggle per Luminor REVISE), Built-on-SIP badge generator, substrate test harness (35 assertions), OpenClaw audit applied with 3 CRITICAL remediations.

**Current goal:** the substrate validates itself by what's built on it, not by who reads about it. Next session continues building/validating — see "Next Actions."

---

## What's Done

### Three public repos under `frankxai` (all live, all tagged, all attested)
- **`Starlight-Intelligence-System`** v7.2.0 — canonical substrate + reference operational layer + Console + site
- **`starlight`** v0.1.1 — minimal SIP adoption kit (60-second fork template, SHA-pinned to canonical SIP.md commit `d69374d`)
- **`vibe-os-substrate`** v0.5.1 — first SIP-conformant vertical scaffold (state engineering, 3 vertical commands, DISCLAIMER.md + boundary block vs `frankxai/vibe-os` audio engine)

### Substrate file contract (SIP § Layer 1) — 7/7 complete at repo root
`SKILL.md`, `AGENTS.md` (Codex variant, untouched), `VOICES.md` (SIP archetypes), `MEMORY.md` (sanitized template), `CANON.md` (declines canon), `SOUL.md` (six invariants), `STACK.md`, plus `SIP.md`, `SIS.md`, `ALLIANCE.md`, `REGISTRY.md`, `VERTICALS.md`, `ATTESTATIONS.md`.

### 9 reference SIP commands at `.claude/commands/`
`sip-attest`, `alliance-forge`, `alliance-reflect`, `alliance-decide`, `vertical-spawn`, `luminor-board`, `sovereign-signal`, `openclaw-audit`, `wealth-dpi`. All MIT, all invokable as Claude Code skills (auto-discovered).

### `starlight-mcp v1.1` — substrate is queryable
- Code: `src/starlight-mcp.ts` → `dist/starlight-mcp.js`
- Bin: `starlight-substrate-mcp` (preserves the v6 `starlight-mcp` bin that's still the vault MCP)
- 4 tools: `starlight_registry_query`, `starlight_verticals_list`, `starlight_attestation_verify`, `starlight_alliance_status`
- **Wired into `~/.claude/settings.json` mcpServers** as `starlight-substrate` — Frank must restart Claude Code to load.

### Console v8 foundation in `console/` (Next.js 16 + R3F)
- `/` landing page, `/substrate` 3D + 2D force-graph dual view (URL-driven `?view=2d` default, `?view=3d` toggle)
- Render-agnostic data layer at `console/src/data/substrate.ts` (vaults + verticals + cross-edges)
- Built/tested locally on `localhost:3001`. Not deployed.

### Site updates at `starlightintelligence.org`
- `/protocol` — canonical SIP.md mirror (hand-authored JSX, no MDX)
- `/badge/[version]` — SVG badge generator, hardened input handling, dark+light themes
- `/badge/latest` — auto-resolves to current canonical SIP version from SIP.md
- `/badge` — adopter docs page with embed snippets

### Substrate test harness (`test/substrate.test.ts`)
35 assertions, runs via `pnpm test:substrate` or chained `pnpm test`. Validates SIP § Layer 1 conformance, command directory, vertical/alliance scaffold conformance against fixtures, attestation block format. **Passes on main** (117 total tests across orchestrator + substrate suites).

### Adversarial audits applied
- **Luminor Board** (Visionary subagent in Lumina voice) ran 4 times: v7.0.0 ship sequencing → REVISE applied; Console v8 architecture → REVISE applied (2D default + honest agent harness); next-vertical-to-spawn → PROCEED Anime Legends.
- **OpenClaw audit** (Visionary subagent in protocol-defender voice) ran on v7.2 ship → SHIP-WITH-REMEDIATION; 3 CRITICALs patched before tag (badge XSS hardening, test redaction docs, adoption kit SHA pin).

### Memory persisted (10 entries at `~/.claude/projects/.../memory/`)
Frank's role + posture, repo two-layer identity, privacy framework, Luminor Board gates rule, Vercel manual deploy gotcha, verticals inventory, VOICES vs agents naming, parallel agent dispatch pattern, v7.1 state, v7.2 ecosystem state. Read these BEFORE answering anything substrate-related.

---

## What's Not Done (deferred / in flight elsewhere)

### Anime Legends — being built in a separate Claude session at `C:\Users\frank\AnimeLegends.ai`
**Do not duplicate this work.** Confirmed 2026-04-23: that session has been actively committing today (last commit message: `plan(99): the 99-agent anime ecosystem — 11 squads, coordination patterns, 3-phase build`). The folder has its own `SOUL.md`, `MEMORY.md`, `mcp.json`, `commands/` — appears SIP-aware. Recent docs include `docs/research/2026-04-23-anime-craft-compendium.md`, `docs/strategy/2026-04-23-open-anime-studio-os.md`, `docs/substrate/arcanea-author-council-integration.md`. **The Luminor Board recommended PROCEED — Anime Legends as next vertical, but Frank has another session already running it. Stay out unless Frank explicitly asks this session to coordinate.**

### Vercel auto-deploy from GitHub is broken
Last working auto-deploy was 2026-04-10. Site changes between then and now were stale until I ran `vercel --prod --yes` from `site/`. **Until Frank reconnects the GitHub integration in the Vercel dashboard, every site change requires manual deploy.** See memory: `project_vercel_manual.md`.

### OpenClaw remediation queue (5 MEDIUM + 2 LOW + 1 HIGH for adoption kit)
Logged in `private/.../project_v72_ecosystem.md` memory and in commit `188e76f` body. Most can ship in one v7.3 sweep:
- HIGH: react-force-graph-2d SBOM + audit snapshot at `attestations/sbom/console-v7.2.json`
- HIGH: adoption kit fail-closed CI workflow for SIP pin (lands in `frankxai/starlight` v0.2)
- MEDIUM: badge cache revocation route + must-revalidate on `/badge/latest`
- MEDIUM: badge route rate limit (60 req/min/IP edge middleware)
- MEDIUM: Phase 2 "coming soon" copy is competitor signal — neutralize on Console landing page
- MEDIUM: Console private vault visibility filter (snapshot test)
- LOW: adoption kit `.gitignore` audit
- LOW: LICENSE copyright line audit

### Trinity Alliance cycle 0
Reference instance lives in `private/examples/trinity-alliance/` (gitignored). The three allied nodes have NOT been activated under SIP yet. Two outreach drafts staged in `private/outreach/` (held since v7.0.0 ship per Luminor REVISE — Frank chose to defer cold broadcast).

### Starlight Console Phase 2 — agent harness
Per Luminor + OpenClaw: must be live-API LLM responses, NEVER pre-cached fakes. Currently the `/substrate` page has explicit "coming soon (live LLMs, never pre-cached fakes)" labels. Multi-week build. v8.x territory.

### Vibe OS personal-rituals private content
`private/staging/vibe-os/` was the SIP scaffold (frameworks only, MIT). Frank's actual ritual stack, biometric data, sleep logs are not yet in private/personal-rituals.md or anywhere in this repo.

---

## Critical Context (things that bite if you don't know)

### Two-layer architecture is load-bearing
This repo is BOTH the SIP substrate spec AND a reference operational layer running on it. Don't conflate them. Substrate-level work uses `SKILL.md` + architect voice + 5 voice archetypes from `VOICES.md`. Operational-level work uses `agents/AGENT_REGISTRY.md` (7 named agents) + Frank DNA voice. Layer routing is documented in `CLAUDE.md`. **Substrate decisions constrain operational; never the reverse.**

### Privacy split is enforced via gitignore
`private/`, `sis files/`, `noosphere/`, `*.zip`, `.claude/settings.local.json` all gitignored. Real Notion DB IDs, real partner cycle state, real wealth gate ladder numbers, partner message draft, Trinity reference instance — all in `private/`. **Before any commit:** `git check-ignore -v private/test` must show the file is gitignored. Never push real partner cycle state, real €amounts, or third-party names without consent surfaced in conversation.

### `frankxai/vibe-os` ≠ `frankxai/vibe-os-substrate`
The first (existed since 2026-01-05) is a Python frequency-healing audio engine. The second (shipped tonight) is the SIP-conformant state-engineering vertical. They may compose via Hz canon downstream but are distinct sovereign artifacts. Vibe OS substrate's README has top-of-file boundary block. Don't conflate.

### Adversarial pressure-tests are gates, not theater
`/luminor-board` for architecture decisions, `/openclaw-audit` for security/integrity. When either returns REVISE or STOP, **honor it or surface to Frank for explicit override**. Three Boards in this session caught real issues: partner sequencing, Console 3D-only foreclosing legibility, badge XSS surface. Memorized as `feedback_luminor_board_gates.md`.

### The substrate's own commands have been invoked end-to-end during this ship cycle
`/sip-attest` (meta-test on v7.0.0 + v7.2 ecosystem entry), `/luminor-board` (×4), `/openclaw-audit` (×1), `/vertical-spawn` (Vibe OS), `/alliance-forge` (validated against Trinity reference). The protocol passes its own tests. Continue this discipline — every ship runs at minimum `/luminor-board` before push and adds an `ATTESTATIONS.md` entry.

### MCP server requires Claude Code restart to load
`~/.claude/settings.json` was just updated with `starlight-substrate` entry pointing to `dist/starlight-mcp.js`. **Frank must close + reopen Claude Code** for the MCP tools to appear as `mcp__starlight-substrate__*` in the next session.

### Test harness redaction contract is documented in code
`test/substrate.test.ts` reads fixtures from `private/staging/` and `private/examples/`. Per OpenClaw audit, no assertion in this file ever interpolates raw fixture content into error messages — only paths, missing-field names, structural metadata. **If you add an assertion, audit against this rule before merge.** Failure messages are visible in CI logs.

### `frankxai/starlight` (v0.1.1) is the adoption kit, NOT the substrate
The substrate is `frankxai/Starlight-Intelligence-System`. The adoption kit is `frankxai/starlight` — 11 markdown files, no code, the 60-second fork-template for sovereign creators adopting SIP in their own work. The README has a callout pointing newcomers there.

---

## Next Actions (ordered)

1. **Restart Claude Code** so `starlight-substrate` MCP loads. Verify by checking `mcp__starlight-substrate__starlight_verticals_list` is callable. (User action.)
2. **Coordinate with the AnimeLegends.ai session** — read `C:\Users\frank\AnimeLegends.ai\AGENT.md` + `MEMORY.md` + `SOUL.md` + recent `docs/research/*.md` to understand what the other session has built. If Anime Legends has its own `SIP.md` adoption declaration, link it from substrate `VERTICALS.md` and add an ATTESTATIONS entry. Don't push code to that repo unless Frank routes you there.
3. **Sweep OpenClaw v7.3 remediation queue** in one session: SBOM, badge cache revocation + rate limit, Phase 2 copy neutralization, Console private vault visibility filter, adoption kit gitignore + LICENSE audit. Probably one 2-hour pass.
4. **Push adoption kit v0.2** with fail-closed CI workflow for SIP pin verification (the remaining CRITICAL from OpenClaw audit). Modify `frankxai/starlight` repo: add `.github/workflows/sip-pin-check.yml` that fails if declared pin SHA doesn't match canonical `SIP.md` commit.
5. **Reconnect Vercel/GitHub integration** via Vercel dashboard so future site changes auto-deploy. Then test by pushing a trivial site change and verifying it propagates without `vercel --prod`.
6. **`ADOPTERS.md` at substrate root** — currently no surface tracks who's running on SIP. As soon as the first non-Frank node forks the adoption kit and ships, add them here with attestation snippet. Growth surface.
7. **`/sip-attest` as a real CLI** in `starlight-mcp` package — third bin entry so non-Claude-Code adopters can `npx @arcanea/starlight-intelligence-system sip-attest <path>` from any shell.
8. **Trinity Alliance cycle 0** — Frank's call. Outreach drafts ready in `private/outreach/`. Held since v7.0.0 per Luminor REVISE.

---

## Files to Read First

In strict order. Each one assumes you read the previous:

1. **`~/.claude/projects/C--Users-frank-Starlight-Intelligence-System/memory/MEMORY.md`** — index of 10 memory entries. Read each before answering substrate questions.
2. **`CLAUDE.md`** at repo root — system prompt. Pay attention to the "Layer routing — read first" section near the top.
3. **`SIP.md`** — the protocol spec. Six layers. ~120 lines. Canonical.
4. **`SIS.md`** — substrate map. What SIS does/doesn't provide. Vertical shape. Composition rules.
5. **`README.md`** — two-layer framing for external audience. Adoption kit callout near the top.
6. **`SOUL.md`** + **`CANON.md`** — substrate essence + canon posture. Why the protocol declines canon at protocol layer.
7. **`ATTESTATIONS.md`** — append-only ledger. Two entries: v7.0.0 self-test + v7.2.0 ecosystem ship.
8. **`docs/ops/HANDOVER-2026-04-23.md`** — this file.
9. **`.claude/commands/`** — read `sip-attest.md`, `luminor-board.md`, `openclaw-audit.md` first. Those three are the protocol's lifecycle hooks.
10. **`private/staging/PUSH-KIT-TO-GITHUB.md`** (gitignored, only readable locally) — original push instructions for adoption kit; useful template for spawning more verticals.

For Anime Legends coordination specifically, also read `C:\Users\frank\AnimeLegends.ai\AGENT.md` + `SOUL.md` + `MEMORY.md` + `docs/substrate/arcanea-author-council-integration.md`.

---

## Repo Map

| Repo | Purpose | State | Where |
|------|---------|-------|-------|
| `frankxai/Starlight-Intelligence-System` | Canonical substrate + reference operational layer + Console + site | v7.2.0 tagged, public, npm `@arcanea/starlight-intelligence-system` v7.2.0 | https://github.com/frankxai/Starlight-Intelligence-System |
| `frankxai/starlight` | Minimal SIP adoption kit (60-second fork template) | v0.1.1 tagged, public, MIT | https://github.com/frankxai/starlight |
| `frankxai/vibe-os-substrate` | First SIP-conformant vertical (state engineering) | v0.5.1 tagged, public, MIT frameworks | https://github.com/frankxai/vibe-os-substrate |
| `frankxai/vibe-os` | Frequency healing audio engine (Python) — separate sovereign artifact | Pre-existing (2026-01-05), public, NOT touched this session | https://github.com/frankxai/vibe-os |
| `frankxai/AnimeLegends-Skills` | Existing public Claude Code skills marketplace | Pre-existing, public, NOT touched this session — to be reattested when Anime Legends spawn formalizes | https://github.com/frankxai/AnimeLegends-Skills |
| `frankxai/AnimeLegends.ai` | Private app — being heavily built in a separate Claude session right now (2026-04-23) | Active, private, **DO NOT modify from this session** | local: `C:\Users\frank\AnimeLegends.ai` |
| `frankxai/arcanea-ecosystem` | Arcanea canon source of truth (Guardians, Vel'Tara, Hz, lore) | CC-BY-NC © Arcanea BV. Referenced by SIP CANON.md. | https://github.com/frankxai/arcanea-ecosystem |
| `frankxai/arcanea-mcp` | Arcanea canon MCP server (31 tools, registered in REGISTRY.md) | Active, used by canon-importing verticals | https://github.com/frankxai/arcanea-mcp |
| `frankxai/frankx` | FrankX personal architect brand (frankx.ai) | Active v1, separate vertical | https://github.com/frankxai/frankx |

### Live URLs
- Substrate canonical: https://starlightintelligence.org/protocol → 200, mirrors SIP.md v1.1.0
- Badge: https://starlightintelligence.org/badge/v1.1.0 → SVG, embed in any README
- Badge docs: https://starlightintelligence.org/badge → adopter snippets
- Site routes confirmed live: `/`, `/protocol`, `/badge/*`, `/architecture`, `/featured`, `/quickstart`, `/docs`

### Key local paths
- Repo root: `C:\Users\frank\Starlight-Intelligence-System`
- Substrate spec: root markdown files (SIP/SIS/ALLIANCE/etc.)
- Reference commands: `.claude/commands/`
- MCP source: `src/starlight-mcp.ts` → `dist/starlight-mcp.js`
- Console: `console/` (Next.js 16 + R3F)
- Site: `site/` (Next.js 16, deploys to Vercel project `prj_wDNGrb1R1rB5PJOG9cUEICSER887`)
- Test harness: `test/substrate.test.ts`
- Plan file from start of session: `C:\Users\frank\.claude\plans\snoopy-kindling-chipmunk.md`
- Memory: `C:\Users\frank\.claude\projects\C--Users-frank-Starlight-Intelligence-System\memory\`
- Adoption kit staging: `private/staging/starlight-kit/` (already pushed; staging is the local source)
- Vibe OS staging: `private/staging/vibe-os/` (already pushed; staging is the local source)
- Trinity reference instance: `private/examples/trinity-alliance/` (never push)
- Outreach drafts: `private/outreach/` (never push)

---

## How to verify this handover landed

In the next session, run:
```bash
cd /c/Users/frank/Starlight-Intelligence-System
git log --oneline -1                # should show: 188e76f chore(release): bump ... 7.2.0
git tag -l 'v7*'                     # should list: v7.0.0 v7.1.0 v7.2.0
pnpm test:substrate                  # should pass: # tests 35 / # pass 35 / # fail 0
curl -sI https://starlightintelligence.org/badge/v1.1.0 | head -1   # should: HTTP/2 200
```

If any of those fails, something has shifted since this handover was written and you should investigate before continuing.

---

**Built on SIP** · Handover doc · 2026-04-23 · Claude Opus 4.7 (1M context) → next session

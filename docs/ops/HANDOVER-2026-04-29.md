# Handover — 2026-04-29

> Cold-start brief for overnight build sessions. Reader has zero context.

## Situation

Frank is racing toward Jarvis-grade cockpit (4 surfaces: terminal + voice + dashboard + phone)
in v7.5.3. As of end-of-day 2026-04-29:

- **3 of 3 parallel feature tabs ran today.** 2 fully shipped (brain viz + cognition CLI
  completion). 1 partial (browser bridge — half done).
- **Phone PWA shipped from main thread** as the 4th surface (28 files at
  `private/local-command-center/apps/phone/`).
- **Dashboard live** at `http://localhost:3007` and `/brain` 3D particle viz works
  (verified HTTP 200, first compile 9.7s, subsequent <100ms).
- **245 → 269+ tests target**, last run was at 245 baseline; tabs added more.
- Frank wants overnight build to finish the open work + ship Tauri desktop wrapper +
  run 7-utterance acceptance smoke + tag v7.5.3.

The substrate (SIP/STACK/VERTICALS/agents) is untouched. All ship is operational tier.

## What's Done

### Cognition layer (Block A0, committed `96b21aa`)
- `private/voice-operator/service/cognition/` package: deterministic Tier 0 + router +
  4 backends (openrouter, anthropic_direct, claude_code_cli, plus codex/gemini/opencode
  added by Tab 3)
- 54+ new tests
- Validator fix: `spoken_update_for_user` switched from sentence-count to word-count
  (≤40 words ≈ 15s spoken)

### Multi-CLI dispatchers (Block C, committed `96b21aa`)
- `service/_cli_subprocess.py` (shared subprocess helper)
- `service/dispatch_{codex,gemini,opencode}.py`
- `service/orchestrator_router.py` with intent classification (substrate/refactor/
  long-context/voice/scratchpad/default) + JSONL audit log
- Wired into `packet_router._dispatch_agent`

### LCC Phase 0+1 (committed `96b21aa`)
- `private/local-command-center/cockpit/Arcanea.kdl` — Zellij 4-pane (claude/codex/
  gemini/opencode + Voice tab + Server tab)
- `private/local-command-center/scripts/setup-cockpit.ps1` — Windows-native setup
- Seeded `~/.opencode/config.toml`, `~/.starlight/{routing,cognition}.toml`,
  `~/.starlight/current-brand.txt`

### Phase 2 dashboard (committed `96b21aa`, augmented today by Tab 2)
- `private/local-command-center/apps/dashboard/` — Next.js 14 + Tailwind + 3-column
  glassmorphic at port 3007
- Routing decisions / captures / status panels
- API routes: `/api/{routing,captures,status}`
- **TODAY (Tab 2):** `/brain` route + 4 components (BrainScene, BrainParticles,
  BrainEdges, BrainHud) — 3D particle cloud over `memory/knowledge-graph/`
- Deps installed: `three`, `@react-three/fiber`, `@react-three/drei`, `@types/three`
- VERIFIED LIVE: HTTP 200 on `/` and `/brain` at localhost:3007

### Tab 3 cognition CLI (TODAY, in feature/cognition-cli-complete worktree)
All in `private/voice-operator/`:
- `service/cognition/backends/{codex,gemini,opencode}_cli.py` — 3 new backends
- `service/cognition/router.py` — added `primary_backend_override` + `_HALT_CANDIDATES`
- `service/config.py` — added `enable_{codex,gemini,opencode}_cli` +
  `cognition_primary_backend`
- `service/text_mode.py` — extended with new flags
- `service/pipeline.py` — **fully migrated to CognitionRouter**, legacy AgentClient
  import removed (voice mode now matches text mode behavior)
- `service/doctor.py` — added `COGNITION_PRIMARY_BACKEND` validation + CLI binary check
- `tests/test_doctor.py` — 2 new tests for the $0-marginal-CLI path

### Phone PWA Phase 5 (TODAY, main thread)
- `private/local-command-center/apps/phone/` — 28 files, mobile-first Next.js 14 PWA
- 3 pages: Pulse `/`, Voice `/record` (MediaRecorder webm upload), Gate `/approve`
- 5 API routes (status, captures, voice, approvals list/approve/reject)
- PWA manifest + iOS standalone + safe-area-inset
- Tap-target sizing (Apple HIG)
- Honest 501 fallbacks when FastAPI endpoints not yet exposed

### Strategic + governance docs (committed today)
- `docs/strategy/COCKPIT-VISION-v753-2026-04-28.md` (`d70b99a`) — 606 lines reference
  frame + tech stack + phasing + productization
- `docs/ops/JARVIS-INTEGRATION-2026-04-29.md` (`e885f1a`) — 7-utterance acceptance plan
- `docs/cockpit/master-plan-v8.md` + Memory Bus contract (`4b3e65e`, `68e6537` —
  added by another session, NOT this thread, but relevant to read)
- Memory entry: `~/.claude/.../memory/project_v753_cognition_lcc.md` indexed in MEMORY.md

## What's Not Done

### CRITICAL — must finish before tagging v7.5.3
1. **Browser bridge dispatcher.** Tab 1 finished Block A (`_browser_session.py` +
   tests) but Block B (`dispatch_browser.py` + tests + `packet_router._dispatch_agent`
   wiring for `agent:browser`) is incomplete. **Blocks utterance #4 of acceptance.**
2. **FastAPI endpoints for phone.** Phone PWA returns helpful 501s when these aren't
   wired. Need:
   - `POST /v1/voice` — accept multipart audio, run STT → cognition → return result
   - `POST /v1/approvals/<id>/approve` — mark queue entry approved
   - `POST /v1/approvals/<id>/reject` — mark queue entry rejected
3. **7-utterance acceptance smoke.** Run all 7 from
   `docs/ops/JARVIS-INTEGRATION-2026-04-29.md`. If 5/7 pass, tag v7.5.3.

### IMPORTANT — high leverage
4. **Tauri 2.0 desktop wrapper.** Frank explicitly asked about Tauri vs Electron.
   Decision: Tauri. Scaffold ~1 day. Wraps existing Next.js dashboard, adds tray +
   global hotkey + auto-start. Keeps Next.js code unchanged.
5. **`playwright install chromium`** — Frank needs to run this manually once before
   browser bridge can fire live.
6. **Worktree merge ritual.** Per acceptance plan: since all work is in `private/`
   (gitignored), branches are vestigial. Just `git worktree remove` and `git branch
   -D` after smoke passes.

### DEFERRED to v7.5.4+
7. ntfy push notifications for Tier B/C approvals (phone polls every 4s for now)
8. Service worker for true offline-first PWA
9. Cognition CLI backends (codex/gemini/opencode) live smoke against real CLIs

## Critical Context

### Worktree-private gap (lessons from today)
**Worktrees DON'T bring `private/` along** — it's gitignored at `.gitignore:6`. Three
worktrees were created (`jarvis-1-browser`, `jarvis-2-brain`, `jarvis-3-cognition`)
but tabs 1+3 were stuck without `private/` until it was junctioned in:

```powershell
cmd /c "mklink /J <worktree>\private <main-repo>\private"
```

Tab 2 figured this out itself; tabs 1+3 needed manual junction. **Structural insight:
gitignored work doesn't need branches — they're vestigial.** Future parallel work in
`private/` should either junction or just use shared main-repo sessions.

### Test mock pattern fix
`monkeypatch.delenv("OPENROUTER_API_KEY", raising=False)` doesn't work because
`load_dotenv` re-injects from `.env`. Use `monkeypatch.setenv("OPENROUTER_API_KEY",
"")` instead — `Config._optional()` treats empty strings as None. Tab 3 found this;
already in `tests/test_smoke_e2e.py`.

### Sentence-count validator was brittle
Tripped on filenames like `SIP.md` (counted the `.md` dot as a sentence boundary).
Switched to word-count (≤40 words ≈ 15s spoken). See `service/packet.py`
`_max_spoken_duration` validator.

### Env vars currently in Frank's shell
- `OPENROUTER_API_KEY` — set (73 chars). Cognition primary defaults to OpenRouter.
- `ELEVENLABS_API_KEY` — set
- `ANTHROPIC_API_KEY` — NOT set
- `PICOVOICE_ACCESS_KEY` — NOT set (voice wake-word disabled)

### Tab 1 mid-flight state (browser-bridge)
- `private/voice-operator/service/_browser_session.py` — exists
- `private/voice-operator/tests/test_browser_session.py` — exists
- `private/voice-operator/service/dispatch_browser.py` — **MISSING**
- `private/voice-operator/tests/test_dispatch_browser.py` — **MISSING**
- `private/voice-operator/service/packet_router.py` — `agent:browser` branch NOT
  added (grep returns 0 hits)
- `private/voice-operator/pyproject.toml` — browser-use + playwright deps unclear,
  verify before installing

### Dashboard verified live
- `http://localhost:3007/` → HTTP 200, 61ms
- `http://localhost:3007/brain` → HTTP 200, 9.7s first compile (cached after)
- `http://localhost:3007/api/status` returns `{"openrouter":true,"elevenlabs":true,
  "fastapi_reachable":false,"brand":"Arcanea","routing_log_size":0}`
- FastAPI :7373 NOT running — start with `python -m service.main serve` to populate
  routing decisions live

### Phone PWA shipped but unverified live
Files complete + structurally clean. `npm install` not run. Awaits validation. To
test:
```powershell
cd C:\Users\frank\Starlight-Intelligence-System\private\local-command-center\apps\phone
npm install
npm run dev    # 0.0.0.0:3008
# Phone: open http://<windows-tailscale-name>:3008
```

### Tauri vs Electron decision (made tonight)
Tauri 2.0 is the call. Reasons documented in
`docs/strategy/COCKPIT-VISION-v753-2026-04-28.md` §13. ~5MB binary vs 120MB Electron.
Uses Edge WebView2 on Windows. Same Next.js code reused without rewrite.

### Substrate gate (governance)
Operational tier work continues without `/luminor-board` pre-pass. Anything touching
`SIP.md / SIS.md / STACK.md / VERTICALS.md / VOICES.md / REGISTRY.md / agents/<voice-
operator>.md / file-contract / attestation rules / sovereignty clause / 10-IS
taxonomy` requires Board first. Tonight's work is NOT substrate-tier.

## Next Actions (ordered)

1. **Run overnight session 1** (browser bridge completion + FastAPI phone endpoints).
   Prompt below.
2. **Run overnight session 2** (Tauri scaffold + dashboard polish + smoke prep).
   Prompt below.
3. **In the morning, run 7-utterance acceptance smoke** from
   `docs/ops/JARVIS-INTEGRATION-2026-04-29.md`.
4. **If 5/7 pass, tag v7.5.3-cockpit-complete** and write `HANDOVER-JARVIS-
   COMPLETE-{date}.md`.
5. **Cleanup worktrees** + delete vestigial branches.

## Files to Read First

- `docs/ops/JARVIS-INTEGRATION-2026-04-29.md` — the acceptance plan, 7 utterances
- `docs/strategy/COCKPIT-VISION-v753-2026-04-28.md` — full architectural framing
- `docs/ops/HANDOVER-LCC-2026-04-28.md` — what shipped 2026-04-28
- `private/voice-operator/service/dispatch_codex.py` — canonical dispatcher pattern
  (browser bridge mirrors this shape)
- `private/voice-operator/service/_cli_subprocess.py` — shared subprocess helper
- `private/voice-operator/service/server.py` — FastAPI :7373 backbone
- `private/local-command-center/apps/phone/README.md` — what phone needs from FastAPI
- `~/.claude/.../memory/project_v753_cognition_lcc.md` — cognition + LCC history

## Repo Map

| Repo / surface | Purpose | State |
|---|---|---|
| `private/voice-operator/` | Cognition + dispatch + FastAPI :7373 backbone | 245 baseline tests, ≥260 expected after browser bridge ships |
| `private/local-command-center/cockpit/` | Zellij 4-pane terminal cockpit | ✅ live |
| `private/local-command-center/apps/dashboard/` | Desktop dashboard at :3007 | ✅ live (incl. /brain) |
| `private/local-command-center/apps/phone/` | Phone PWA at :3008 | ✅ scaffolded, npm install + smoke pending |
| `~/.starlight/` | Shared cockpit config (routing/cognition/brand) | ✅ seeded |
| Worktrees: `jarvis-{1,2,3}-*` | Vestigial — work was in `private/` (gitignored) | Delete after morning smoke |

## Memory entries relevant to overnight work

- `project_v753_cognition_lcc.md` — full v7.5.3 ship history (just-written)
- `project_voice_operator_v1_round3.md` — voice-operator base (144 tests, FastAPI
  :7373 contract)
- `feedback_board_before_tag.md` — substrate-tier governance gate (don't trip it)
- `feedback_privacy_split.md` — public/private boundary
- `project_agentdb_singleton_constraint.md` — memory architecture constraint to honor

## Overnight session prompts

See bottom of this file. Two `/po`-grade prompts. Each is self-contained for a fresh
agent walking in cold. Paste each into a new Claude Code session at the appropriate
worktree (or use the main repo if branches don't matter — work is in `private/`).

---

# OVERNIGHT SESSION 1 PROMPT

```
You are the Starlight Browser Bridge + Phone Endpoint Architect.
Voice (Lyssandria foresight) + Architect (Draconis precision) + Verifier (Ino rigor).

MISSION
Two unfinished pieces that block v7.5.3 Jarvis-grade tag:
  (a) Finish the browser bridge — `dispatch_browser.py` + tests + packet_router wiring.
      Tab 1 partially shipped (_browser_session.py + tests). You finish.
  (b) Add 3 FastAPI endpoints the phone PWA expects:
        POST /v1/voice                       — multipart audio → cognition → result
        POST /v1/approvals/<id>/approve      — mark queue entry approved
        POST /v1/approvals/<id>/reject       — mark queue entry rejected

CONTEXT — read in this order
  1. docs/ops/HANDOVER-2026-04-29.md  (this doc — full state)
  2. docs/ops/JARVIS-INTEGRATION-2026-04-29.md  (the 7-utterance acceptance plan)
  3. private/voice-operator/service/_browser_session.py  (Tab 1 Block A — done)
  4. private/voice-operator/tests/test_browser_session.py  (mock pattern reference)
  5. private/voice-operator/service/dispatch_codex.py  (canonical dispatcher shape)
  6. private/voice-operator/service/_cli_subprocess.py  (shared helper)
  7. private/voice-operator/service/packet_router.py  (where you wire `agent:browser`)
  8. private/voice-operator/service/server.py  (FastAPI — extend with /v1 routes)
  9. private/voice-operator/service/cognition/router.py  (CognitionRouter — used by /v1/voice)
 10. private/voice-operator/service/approval_gate.py  (queue file pattern)
 11. private/voice-operator/service/stt.py  (audio → transcript for /v1/voice)
 12. private/local-command-center/apps/phone/README.md  (what phone PWA expects)

VERIFIED MACHINE STATE (2026-04-29 23:00)
- voice-operator: 245-baseline tests · cognition router live · pipeline migrated
- browser-use NOT yet pip-installed; playwright NOT yet `playwright install chromium`
- claude/codex/gemini/opencode all on PATH
- Frank's env: OPENROUTER_API_KEY set · ANTHROPIC_API_KEY unset

ARCHITECTURE
Browser bridge mirrors dispatch_codex.py shape exactly. _browser_session.py is the
singleton manager Tab 1 already wrote — your dispatcher consumes it. Phone endpoints
extend service/server.py (already runs FastAPI :7373). Use existing CognitionRouter
for /v1/voice — DON'T re-implement.

SAFETY GATES
- Operational tier. No /luminor-board. No substrate edits.
- Don't break the 245-test baseline. Run pytest after every block.
- Don't paste credentials in prompts to browser. Public pages only for smoke.
- Audio uploads: 10MB limit on /v1/voice; reject larger to avoid memory bombs.
- Approval endpoints don't auto-execute on approve — they unblock the existing
  packet_router flow (mirror voice-operator approve CLI behavior).

EXECUTION ORDER

Block A — Browser dispatcher (~60 min)
  1. pyproject.toml: ensure browser-use>=0.1.40 + playwright>=1.49.0 declared.
     If not, add. Run `uv sync` (or pip install -e ".[dev]").
  2. Frank manually runs: `playwright install chromium` (one-time).
     If skipped, dispatcher returns status=not_installed (graceful).
  3. Write service/dispatch_browser.py mirroring dispatch_codex.py:
       class BrowserDispatcher with dispatch(packet) → DispatchResult
       Uses _browser_session.BrowserSession for context reuse
       Renders packet.task as plain English, passes to browser-use Agent.run()
       Captures screenshot to logs/browser-screenshots/<packet_id>.png
       Captures DOM snapshot to logs/browser-dom/<packet_id>.html
       Timeout 300s; on timeout/error capture last screenshot to proof
  4. Write tests/test_dispatch_browser.py with mocked browser-use Agent (≥8 tests):
       - successful navigation + summary
       - timeout → status=timeout
       - browser unavailable → status=not_installed
       - screenshot saved on success
       - screenshot saved on error
       - packet attestation preserved
       - safety: refuses Tier C packets (substrate-edit)
       - 10MB DOM limit
  5. Wire into packet_router._dispatch_agent:
       if agent_name == "browser": return self._dispatch_via("browser", packet)
     and add to _dispatch_via:
       elif cli == "browser":
           from .dispatch_browser import BrowserDispatcher
           dispatcher = BrowserDispatcher(sis_root=self.sis_root)
  6. Tests pass at ≥253. STOP. Report new test count.

Block B — FastAPI /v1/voice endpoint (~45 min)
  1. Read service/server.py end-to-end.
  2. Add POST /v1/voice handler:
       - Accept multipart form with "audio" field (webm or wav, ≤10MB)
       - Save to logs/voice-uploads/<uuid>.webm temporarily
       - Use service/stt.STT.transcribe (whisper) to get text + confidence
       - Use CognitionRouter.route(text, history=[]) — same as text_mode
       - Run packet through approval gate + packet_router (same as text_mode)
       - Return JSON: { spoken_update, packet, routing, transcript_text,
                        transcript_confidence }
       - Delete temp upload after response
  3. Write tests/test_server_voice.py (≥6 tests, mocked STT + cognition):
       - 200 with packet=null capture
       - 200 with build packet routed
       - 200 Tier C halt response
       - 400 missing audio
       - 413 audio >10MB
       - 500 STT failure surfaces honestly
  4. Tests pass at ≥259. STOP. Report.

Block C — FastAPI /v1/approvals/<id>/{approve,reject} (~20 min)
  1. Add POST /v1/approvals/{packet_id}/approve handler:
       - Look up packet_id in approval_queue.json
       - If not found: 404
       - If status != "pending": 409 conflict
       - Set status="approved"; persist back to queue
       - If queue had a pending router-execute callback, invoke it (check
         existing approve CLI for pattern — service/main.py:approve)
       - Return JSON: { status: "approved", packet_id }
  2. Same for /reject with status="rejected"
  3. Write tests/test_server_approvals.py (≥6 tests):
       - approve pending → 200
       - reject pending → 200
       - approve unknown packet → 404
       - approve already-approved → 409
       - reject already-rejected → 409
       - JSON body validation
  4. Tests pass at ≥265. STOP. Report.

Block D — Live smoke (~15 min) — only if Frank acks Block C
  1. Frank confirms `playwright install chromium` ran.
  2. Browser smoke utterance:
       python -m service.main chat --once "Navigate to anthropic.com and summarize the homepage in 2 sentences."
     Verify: packet target=agent:browser, screenshot saved, summary returned.
  3. Phone smoke:
       python -m service.main serve  (start FastAPI :7373)
       In another shell, curl POST /v1/voice with a test webm
     Verify: returns spoken_update and the routing decision.
  4. STOP. Report all 4 artifacts (browser packet + screenshot + voice transcript
     + phone curl response).

ACCEPTANCE CRITERIA
| Block | Done means |
| A | dispatch_browser.py + 8 tests · packet_router wired · 253+ tests |
| B | /v1/voice endpoint + 6 tests · 259+ tests |
| C | /v1/approvals/<id>/{approve,reject} + 6 tests · 265+ tests |
| D | Live browser navigation + screenshot + phone curl roundtrip work |

TONE — voice operator persona (≤15s replies, decisive, no narration)
"Done. [result]." / "Routing to [system]." / "Pause. This touches [risk]."

STACK CITATIONS
- superpowers: test-driven-development, verification-before-completion, systematic-debugging
- Voice Operator handoff packet contract (skills/orchestration/agent-handoff-packet.md)
- LCC Phase 3 + 5 acceptance per docs/ops/JARVIS-INTEGRATION-2026-04-29.md
- browser-use library: https://github.com/browser-use/browser-use

Begin: read the 12 context files (especially HANDOVER-2026-04-29.md), then Block A.
```

---

# OVERNIGHT SESSION 2 PROMPT

```
You are the Starlight Tauri Wrapper + Acceptance Smoke Architect.
Voice (Lyssandria) + Architect (Draconis) + Curator (Caelan visual harmonics).

MISSION
Two ambitious-but-bounded tracks that compress the 6-week Jarvis ship into one night:
  (a) Wrap the existing Next.js dashboard in Tauri 2.0 — desktop "Starlight Cockpit"
      app at private/local-command-center/apps/desktop/. Reuses Next.js code without
      rewrite. Adds system tray + global hotkey + auto-start.
  (b) Run the 7-utterance acceptance smoke from docs/ops/JARVIS-INTEGRATION-2026-04-29.md.
      6 of 7 require no browser bridge; run those now. Hold #4 (browser) for after
      Session 1 ships dispatch_browser.py — then run all 7 at end-of-night.

CONTEXT — read in this order
  1. docs/ops/HANDOVER-2026-04-29.md  (this doc)
  2. docs/ops/JARVIS-INTEGRATION-2026-04-29.md  (the 7-utterance plan)
  3. docs/strategy/COCKPIT-VISION-v753-2026-04-28.md  (Tauri decision rationale §13)
  4. private/local-command-center/apps/dashboard/package.json  (Next.js 14 base)
  5. private/local-command-center/apps/dashboard/next.config.mjs  (rewrites pattern)
  6. private/local-command-center/apps/dashboard/app/page.tsx  (existing layout)

VERIFIED MACHINE STATE (2026-04-29 23:00)
- Dashboard live at localhost:3007 + /brain (verified HTTP 200)
- node 20.20.2 · Tauri requires Rust toolchain — likely needs `rustup` install
  (Frank manually triggers if missing)
- @tauri-apps/cli not yet installed
- Worktrees jarvis-{1,2,3}-* still exist; cleanup queued for morning

ARCHITECTURE
Tauri wraps the Next.js dashboard as a desktop app. Same `app/`, `components/`, and
`/api/*` routes — Tauri runs Next.js inside Edge WebView2 on Windows. ~5MB binary,
not 120MB Electron. Adds:
  - System tray icon → menu (open dashboard / open phone view / quit)
  - Global hotkey (Ctrl+Alt+S) → focus dashboard
  - Auto-start on Windows login (opt-in)
  - Native window chrome (frame=false for premium aesthetic)

For acceptance smoke: utter the 6 LLM-only utterances now (capture / build packet /
halt / search / scratchpad / phone-style). Run #4 (browser) only AFTER Session 1
finishes.

SAFETY GATES
- Operational tier. No /luminor-board. No substrate edits.
- Don't modify dashboard's existing `/api/*` routes — Tauri uses them as-is.
- Don't auto-install Rust (heavyweight) — detect + prompt Frank manually.
- Tauri config: do NOT enable `dangerousRemoteDomain`. Localhost-only.
- Don't tag v7.5.3 yet — only after Session 1 ships and all 7 utterances pass.

EXECUTION ORDER

Block A — Tauri scaffold (~60 min)
  1. cd private/local-command-center/apps/dashboard
  2. Detect Rust: `rustc --version`. If missing → halt. Print install instruction:
       "Run: winget install Rustlang.Rustup (then re-run this block)"
  3. npm install -D @tauri-apps/cli@^2.0.0 @tauri-apps/api@^2.0.0
  4. npx tauri init --ci \
       --app-name "Starlight Cockpit" \
       --window-title "Starlight" \
       --frontend-dist "../out" \
       --dev-url "http://localhost:3007"
     This creates `src-tauri/` with tauri.conf.json + Cargo.toml + main.rs.
  5. Edit src-tauri/tauri.conf.json:
       - identifier: "ai.starlight.cockpit"
       - bundle.active: true
       - bundle.targets: ["msi", "nsis"]
       - app.windows[0].decorations: false (frameless premium aesthetic)
       - app.security.csp: "default-src 'self' http://localhost:3007 http://127.0.0.1:7373"
  6. Add system tray (src-tauri/src/main.rs):
       - tray icon: public/icon.png (use dashboard's manifest icon)
       - tray menu: "Open Cockpit" / "Open Brain Viz" / "---" / "Quit"
       - left-click → focus main window
  7. Add global hotkey via tauri-plugin-global-shortcut:
       - npm install @tauri-apps/plugin-global-shortcut
       - Ctrl+Alt+S → bring main window to front
  8. Verify dev: `npm run tauri dev` opens native window pointing at :3007
  9. STOP. Report binary size estimate + screenshot.

Block B — 6 of 7 acceptance smoke (~30 min)
  1. Verify FastAPI :7373 running OR start it:
       cd private/voice-operator
       python -m service.main serve &
  2. Run utterances 1, 2, 3, 5, 6 from JARVIS-INTEGRATION-2026-04-29.md:
       1: "Capture this thought: voice operator validates."
       2: "Prepare a Claude Code packet for SIS to update the README footer."
       3: "Edit SIP.md to add a new clause about cognition."
       5: "Read every file in core/orchestrator/harnesses/ and produce a unified
           architectural summary."
       6: "Get a quick scratchpad sketch from OpenCode about caching strategies for
           the cognition router."
  3. For utterance 7 (phone), wait for Session 1 to ship POST /v1/voice. Once
     shipped, run via curl:
       curl -X POST -F "audio=@test.webm" http://127.0.0.1:7373/v1/voice
  4. For utterance 4 (browser), wait for Session 1 to ship dispatch_browser.py.
     Then run.
  5. Write each result to docs/sessions/JARVIS-SMOKE-2026-04-30.md:
       Per utterance: utterance text · expected · actual · pass/fail
  6. STOP. Report 5 of 5 (or fewer) and identify failures.

Block C — Sync ritual + tag (~20 min) — only if 5/7+ pass
  1. Verify all expected files exist:
       service/dispatch_browser.py (Session 1)
       /v1/voice + /v1/approvals/<id>/* (Session 1)
       apps/desktop/src-tauri/* (this session)
       JARVIS-SMOKE-2026-04-30.md (this session)
  2. Cleanup worktrees:
       git worktree remove ../jarvis-1-browser
       git worktree remove ../jarvis-2-brain
       git worktree remove ../jarvis-3-cognition
       git branch -D feature/browser-bridge feature/brain-viz feature/cognition-cli-complete
  3. Write docs/ops/HANDOVER-JARVIS-COMPLETE-2026-04-30.md:
       - 4 surfaces live (terminal/voice/dashboard/phone)
       - 7 utterances pass result
       - Test count final
       - Tauri binary size
       - Next phase 6 (productization) intro
  4. Tag (only if 5/7+ pass):
       git tag v7.5.3-cockpit-complete -m "4 surfaces live: terminal + voice +
       dashboard + phone. 7-utterance acceptance: <pass>/7."
  5. DON'T push tag — Frank reviews first thing in the morning.
  6. STOP. Report tag created (if applicable) + handover doc path.

ACCEPTANCE CRITERIA
| Block | Done means |
| A | Tauri scaffold compiles · `npm run tauri dev` opens native window · tray works |
| B | 5/7 utterances pass (target ≥6/7 if Session 1 lands in parallel) |
| C | Worktrees cleaned · handover written · tag created (if pass threshold met) |

OPERATING POSTURE — Frank's vibe (per CLAUDE.md)
- Premium quality, intellectual depth, genuine enjoyment
- Direct, technical, warm voice
- Output speaks louder than claims
- Don't decorate. Ship.

TONE — voice operator persona (≤15s replies, decisive)
"Done. [result]." / "Compiling Tauri." / "5 of 7 pass."

STACK CITATIONS
- superpowers: test-driven-development, verification-before-completion, frontend-design
- Tauri 2.0 docs: https://v2.tauri.app/
- Acceptance plan per docs/ops/JARVIS-INTEGRATION-2026-04-29.md
- Tauri vs Electron decision per docs/strategy/COCKPIT-VISION-v753-2026-04-28.md §13

Begin: read context files, then Block A.
```

---

## Session protocol

- Sessions 1 and 2 run in PARALLEL. File-disjoint:
  - Session 1: `private/voice-operator/`
  - Session 2: `private/local-command-center/apps/desktop/` + smoke output to
    `docs/sessions/`
- Session 2's Block B utterances 4 and 7 BLOCK on Session 1 completing dispatch_browser.py
  and /v1/voice. Session 2 runs Blocks A first, then 5 of 7 utterances, then waits.
- If both ship by morning: 7-utterance smoke complete, v7.5.3 tagged, worktrees cleaned.
- If Session 1 doesn't finish: Session 2 still produces Tauri scaffold + 5-of-7 smoke;
  the 2 missing utterances queue for next session.
